# ===========================
# Cursor Rules for Go2Asia
# ===========================

version: 1

# ---------------------------
# ⚠️ ВАЖНО: PowerShell и Git операции
# ---------------------------

## PowerShell (НЕ bash!)

**КРИТИЧЕСКИ ВАЖНО:** У пользователя Windows PowerShell, а не bash!

### Правила работы с командами:

1. **НЕ используй Unix-команды:**
   - ❌ `grep`       → ✅ `Select-String`
   - ❌ `sed`, `awk` → ✅ PowerShell-эквиваленты
   - ❌ `cat`, `| cat` → ✅ обычный вывод или `| Out-String`

2. **Все команды должны быть PowerShell-совместимыми.**

### Git операции (Только через пользователя):

**НЕ выполняй автоматически следующие команды:**
- ❌ `git push`
- ❌ `git pull`
- ❌ `git fetch`
- ❌ `git merge`
- ❌ `git checkout -b`

**Вместо этого:**
- ✅ Пиши пользователю список команд PowerShell для выполнения.
- ✅ Жди, пока пользователь сам их выполнит.
- ✅ НЕ выполняй эти команды автоматически через `run_terminal_cmd`.

### Проверка после операций с ветками:

**ОБЯЗАТЕЛЬНО** после любых операций с ветками добавляй команды проверки:

```powershell
git branch -a
git branch -r | Select-String "feature/"
```

**ВАЖНО:** Анализируй РЕАЛЬНЫЙ вывод команд, а не предполагаемое состояние.

### Пример правильного подхода:

Опиши пользователю:

```markdown
Выполните следующие команды PowerShell:

```powershell
git checkout main
git pull origin main
git checkout -b feature/space-asia
git add apps/go2asia-pwa-shell/components/space/
git commit -m "feat(space): базовая структура"
git push -u origin feature/space-asia
```

После выполнения проверьте:
```powershell
git branch -a
git branch -r | Select-String "feature/"
```
```

# ---------------------------
# CAPSULES CONFIG
# ---------------------------

capsules:
  # ---- MAIN APP SHELL ----
  - name: app-shell
    root: apps/go2asia-pwa-shell
    readOnly: false
    description: >
      Основной фронтенд Go2Asia. Разрешено редактирование,
      создание компонентов, страниц и интеграций.
    priority: high

  # ---- BACKEND SERVICES ----
  - name: services
    root: services
    readOnly: false
    description: >
      Все микросервисы Phase 0–1 (Auth, Token, Referral, Content и др.).
      Cursor может обновлять логику, роуты, миграции и тесты.
    priority: high

  # ---- PROTOTYPES (BOLT.NEW) ----
  - name: prototypes
    root: prototypes
    readOnly: true
    ignoreGit: true
    description: >
      READ-ONLY. Прототипы Bolt.New.
      Cursor может читать эти файлы, анализировать их, брать UI/UX-паттерны,
      Tailwind-классы, компоненты, структуры, но НЕ должен изменять файлы.
      Используется только как эталонный источник UI.
    priority: reference

  # ---- SHARED PACKAGES ----
  - name: packages
    root: packages
    readOnly: false
    description: >
      Общие пакеты: SDK, types, UI-компоненты и утилиты.
      Можно расширять, но аккуратно — важно не ломать публичный API.
    priority: medium

  # ---- DOCUMENTATION ----
  - name: docs
    root: docs
    readOnly: false
    description: >
      Документация проекта. Cursor может обновлять и расширять.
    priority: low

  # ---- DESIGN SYSTEM (READ-ONLY) ----
  - name: design-system
    root: design-system
    readOnly: true
    ignoreGit: true
    description: >
      Внешние файлы дизайн-системы, сгенерированные Bolt.New.
      Cursor НИКОГДА не должен модифицировать эти файлы.
      Cursor может только читать их как reference для реализации UI.
    priority: reference

# ---------------------------
# GLOBAL RULES
# ---------------------------

rules:

  # ---- GIT IGNORED CONTENT ----
  - pattern: "/prototypes/**"
    action: allow-read
    description: >
      Прототипы Bolt.New можно читать, анализировать и использовать
      как визуальный и структурный reference.
      Изменять — нельзя.

  # ---- PROTECT PROTOTYPES ----
  - pattern: "/prototypes/**"
    action: prevent-write
    description: >
      Категорически запрещено изменять, переименовывать, удалять
      или создавать файлы в prototypes/.

  # ---- PROTECT PROTOTYPES FROM COMMITS ----
  - pattern: "/prototypes/**"
    action: prevent-commit
    description: >
      Файлы прототипов никогда не должны попасть в GitHub.

  # ---- FRONTEND APP SHELL RULES ----
  - pattern: "apps/go2asia-pwa-shell/**"
    action: allow-edit
    description: >
      Разрешено создавать/модифицировать компоненты, страницы,
      layout, хелперы, хуки и интеграции.

  # ---- BACKEND SERVICES RULES ----
  - pattern: "services/**"
    action: allow-edit
    description: >
      Разрешено менять код микросервисов, типы, роуты и тесты.

  # ---- SHARED PACKAGES RULES ----
  - pattern: "packages/**"
    action: allow-edit
    description: >
      Cursor может обновлять SDK, types и UI kit.

  # ---- DOCUMENTATION RULES ----
  - pattern: "docs/**"
    action: allow-edit
    description: >
      Документация может свободно редактироваться Cursor.

  # ---- PLANS STRUCTURE RULES ----
  - pattern: "docs/plans/**"
    enforce:
      - "Использовать структуру phase_X/stage_Y/sprint_Z"
      - "Файлы должны иметь имена: phase_XXX_plan.md, stage_XXXX_plan.md, sprint_XXX_plan.md"

  # ---- REPORTS STRUCTURE RULES ----
  - pattern: "docs/reports/**"
    enforce:
      - "Следовать структуре: phase_XXX_report.md, stage_XXXX_report.md, sprint_XXX_report.md"

# ---------------------------
# SAFETY & SANITY RULES
# ---------------------------

sanity:

  # Не трогать node_modules
  - pattern: "**/node_modules/**"
    action: prevent-any

  # Не трогать билд артефакты
  - pattern: "**/dist/**"
    action: prevent-any

  # Не генерировать ненужные lock-файлы
  - pattern: "**/package-lock.json"
    action: prevent-write

  # Только pnpm + обязательное чтение документации и защита структуры
  - pattern: "*"
    enforce:
      - "использовать pnpm во всех командах"
      - "Перед генерацией кода обязательно изучи соответствующие документы из docs/, включая ADR."
      - "Запрещено изменять структуру директорий (переименовывать, перемещать, удалять), если это не утверждено ADR."
      - "Любое предложение изменения структуры → сначала создать ADR в docs/decisions/."
      - "Все DevOps-настройки должны находиться в docs/ops/, а не в новых директориях."
      - "Secrets никогда не создаются и не выводятся в код. Использовать placeholders вида <SECRET_ENV>."
      - "Если есть сомнения, какой модуль/сервис затронут — уточни у пользователя."
      - "Перед изменением CI/CD или Cloudflare-конфигурации обязательно прочитай docs/ops/environments.md и docs/ops/cloudflare_setup.md"

# ---------------------------
# DESIGN SYSTEM RULES
# ---------------------------

## 🎨 Дизайн-система: Единственный источник правды

### ⚠️ КРИТИЧЕСКИ ВАЖНО: Два источника правды для компонентов

**packages/ui — ЕДИНСТВЕННЫЙ рабочий источник компонентов  
design-system/ — READ-ONLY документация и архив прототипов**

### Правила работы с компонентами:

#### ✅ ОБЯЗАТЕЛЬНО:

1. **Все изменения компонентов ТОЛЬКО в `packages/ui/`**
   - Редактировать компоненты: `packages/ui/src/components/*`
   - Добавлять новые компоненты: `packages/ui/src/components/`
   - Обновлять Storybook: `packages/ui/src/components/*/*.stories.tsx`

2. **`design-system/` — ТОЛЬКО для чтения**
   - НЕ редактировать компоненты в `design-system/ui-components/*.tsx`
   - НЕ синхронизировать компоненты из `packages/ui` в `design-system/`
   - Использовать только для:
     - чтения документации (`DESIGN_SYSTEM.md`, `DESIGN_TOKENS_GUIDE.md`)
     - референса дизайн-токенов (`design-tokens.json`)
     - референса конфигурации Tailwind (`tailwind.config.full.js`)

3. **Импорты компонентов ТОЛЬКО из `@go2asia/ui`**

```tsx
// ✅ Правильно
import { Button, Card, ModuleTile } from '@go2asia/ui';

// ❌ НЕПРАВИЛЬНО
import { Button } from '../../design-system/ui-components/Button';
```

#### ❌ ЗАПРЕЩЕНО:

1. Редактировать файлы в `design-system/ui-components/`
2. Создавать локальные копии компонентов в `apps/go2asia-pwa-shell/components/`
3. Импортировать компоненты из `design-system/`
4. Синхронизировать компоненты между `packages/ui` и `design-system/`

### Структура дизайн-системы:

```text
packages/ui/                    # ← РАБОЧИЙ источник правды
├── src/components/             # ← Редактировать здесь
├── .storybook/                 # ← Storybook здесь
└── README.md

design-system/                  # ← READ-ONLY документация
├── DESIGN_SYSTEM.md            # ← Читать документацию
├── DESIGN_TOKENS_GUIDE.md      # ← Референс токенов
├── design-tokens.json          # ← Референс токенов
├── tailwind.config.full.js     # ← Референс конфигурации
└── ui-components/              # ← НЕ РЕДАКТИРОВАТЬ (архив)
```

### Процесс добавления/изменения компонента:

1. Редактировать компонент в `packages/ui/src/components/ComponentName/`.
2. Обновить Storybook в `packages/ui/src/components/ComponentName/ComponentName.stories.tsx`.
3. При необходимости обновить документацию в `packages/ui/README.md`.
4. НЕ трогать `design-system/ui-components/`.

---

## 📁 Структура монорепозитория

### Основные директории:

- `apps/`          — Frontend приложения (только go2asia-pwa-shell)
- `services/`      — Backend микросервисы (Cloudflare Workers)
- `packages/`      — Общие пакеты (ui, sdk, types, config, logger)
- `docs/`          — Документация проекта
- `design-system/` — READ-ONLY документация дизайн-системы
- `scripts/`       — Вспомогательные скрипты
- `tests/`         — E2E тесты

### Правила работы:

1. Монорепо верхнего уровня: `apps/`, `services/`, `packages/`, `docs/` — базовая структура.
2. PWA Shell: `apps/go2asia-pwa-shell/` — единственное frontend-приложение.
3. Микросервисы: `services/*` — Cloudflare Workers.
4. UI-kit: `packages/ui` — единственный источник правды для компонентов.
5. OpenAPI-first: `docs/openapi` → `packages/types` + `packages/sdk`.

---

## 🔧 TypeScript и типы

### Генерация типов:

- Типы генерируются из OpenAPI: `pnpm gen:types`.
- SDK генерируется из OpenAPI: `pnpm gen:sdk`.
- НЕ редактировать вручную: `packages/types/src/index.ts`, `packages/sdk/src/*.ts`.

### Использование типов:

```tsx
// ✅ Правильно
import { User } from '@go2asia/types';
import { useGetUser } from '@go2asia/sdk';

// ❌ НЕПРАВИЛЬНО
import { User } from './types/user'; // Локальные типы
```

---

## 🎨 Типографика (зафиксирована согласно прототипу Bolt.New)

### Размеры заголовков:

- H1: `text-h1 md:text-4xl lg:text-5xl` (30px / 36px / 48px) + `mb-3 md:mb-4`
- H2: `text-h2 md:text-3xl` (24px / 30px)
- H3: `text-h3 md:text-2xl` (20px / 24px)

### Grid модулей:

- Классы: `grid grid-cols-2 md:grid-cols-3 gap-3 md:gap-4`
- Mobile: 2 колонки (9 модулей = 5 рядов)
- Tablet/Desktop: 3 колонки (9 модулей = 3 ряда)

---

## 📚 Документация

### Источники правды:

- UI/UX Style Guide: `docs/overview/go2asia_style_guide.md`
- Компоненты: `packages/ui/` + Storybook
- Дизайн-система: `design-system/DESIGN_SYSTEM.md` (read-only)
- Структура проекта: `docs/README.md`
- Карта контекста для Cursor: `docs/ai/context_map_for_cursor.md`

---

## ✅ Чек-лист перед коммитом

- [ ] Все компоненты импортируются из `@go2asia/ui`.
- [ ] НЕ редактировались файлы в `design-system/ui-components/`.
- [ ] Обновлён Storybook для изменённых компонентов.
- [ ] Типографика соответствует прототипу Bolt.New.
- [ ] Grid модулей: `grid-cols-2 md:grid-cols-3`.
- [ ] Проверен линтинг: `pnpm lint`.
- [ ] Проверены типы: `pnpm typecheck`.

# ---------------------------
# AI MULTI-AGENT CONTEXT (Go2Asia)
# ---------------------------

- В этом репозитории используется **мультиагентная модель разработки** для Go2Asia.
- Все описания ролей, пайплайнов и правил работы ИИ-агентов находятся в `docs/ai/`.

### Что должен делать Cursor перед выполнением сложной задачи

1. **Понять тип задачи**  
   - архитектура, фронтенд, backend, DevOps, дизайн, контент, мультиагентка.

2. **Прочитать соответствующие документы** (см. карту контекста):
   - `docs/ai/context_map_for_cursor.md` — отправная точка.
   - ADR по теме (из `docs/decisions/` и `docs/ai/decisions/`).
   - Профильные файлы из:
     - `docs/overview/`
     - `docs/architecture/`
     - `docs/modules/`
     - `docs/backend/`
     - `docs/ops/`
     - `docs/design/`
     - `docs/content/`.

3. **Соблюдать роли и пайплайны ИИ-агентов**
   - Описание ролей: `docs/ai/roles/*.md`.
   - Пайплайны и сценарии: `docs/ai/workflows/*.md`.
   - Решения по мультиагентной архитектуре: `docs/ai/decisions/adr_*.md`.

4. **Не вводить новые директории и слои для MVP**
   - Следовать `docs/ai/decisions/adr_0003_no_extra_directories_for_mvp.md`.

### Дополнительные указания для агентов

- **Оркестратор**: всегда сначала читает ADR и overview-документы, затем распределяет задачи между ролями.
- **ИИ-архитектор**: работает только в рамках принятых ADR, новые предложения всегда оформляет как черновик ADR.
- **ИИ-бэкенд-разработчик**: сверяется с `api_architecture.md`, `be_architecture.md`, профильным сервисом в `docs/backend/`.
- **ИИ-фронтенд-разработчик**: сверяется с `design/`, `modules/{module}/`, UI-kit в `packages/ui`.
- **ИИ-devops-инженер**: использует только `docs/ops/*` и не создаёт новые DevOps-директории.
- **ИИ-технический писатель**: базируется на `docs/content/*` и `docs/overview/*`.

### Важные правила для Cursor

- Всегда сверяйся с ADR перед изменением архитектуры, структуры репо, схемы БД или API.
- При работе в конкретном модуле (например, Atlas) ограничивай контекст:
  - `docs/modules/atlas/`
  - соответствующий сервис в `docs/backend/`
  - нужные архитектурные и дизайн-документы.
- Не добавляй новые роли AI-агентов и новые рабочие директории без явного запроса от пользователя.

# ---------------------------
# REVIEW PIPELINE POLICY
# ---------------------------

review_policy:
  description: >
    Правила, по которым Cursor/Оркестратор должны запускать режимы ревью
    (sub-agent / review-mode) при работе над ключевыми артефактами.

  triggers:
    - name: tz_review
      when:
        - "Создаётся или существенно изменяется ТЗ (docs/modules/**, docs/backend/**)"
      agent: "Requirements Analyst"
      mode: "Review / Sub-agent Mode: Ревью технического задания (TЗ)"
      must_write_to: "docs/reviews/tz/"
      docs:
        - "docs/ai/workflows/review_pipeline.md#21-техническое-задание-tз"

    - name: architecture_review
      when:
        - "Создаётся или меняется архитектура сервиса/модуля (docs/architecture/**, docs/backend/**/architecture.md)"
      agent: "Architect"
      mode: "Review / Sub-agent Mode: Архитектурное ревью"
      must_write_to: "docs/reviews/architecture/"
      docs:
        - "docs/ai/workflows/review_pipeline.md#22-архитектура"

    - name: plan_review
      when:
        - "Создаётся план Фазы/Этапа/Спринта (docs/plans/**)"
      agent: "Planner"
      mode: "Review / Sub-agent Mode: Ревью плана разработки"
      must_write_to: "docs/reviews/plans/"
      docs:
        - "docs/ai/workflows/review_pipeline.md#23-план-разработки-фаза--этап--sprint"

    - name: code_review
      when:
        - "Вносятся значимые изменения в backend (services/**)"
        - "Меняются пакеты packages/sdk/** или packages/types/**"
        - "Меняются критичные компоненты в packages/ui/**"
      agents:
        - "Backend Dev"
        - "QA"
        - "Security"
      mode: "Review / Sub-agent Mode: Код-ревью (Backend + QA + Security)"
      must_write_to: "docs/reviews/code/"
      docs:
        - "docs/ai/workflows/review_pipeline.md#24-код-backend-критичный-frontend-sdk"

  rules:
    - "Оркестратор не должен считать задачу завершённой, если при срабатывании одного из триггеров отсутствует соответствующий review-файл."
    - "При статусе 'changes required' из review-файла задача должна быть доработана и отправлена на повторное ревью."
    - "Перед любыми архитектурными изменениями Cursor обязан свериться с review_pipeline.md."

# ---------------------------
# REVIEW SUB-AGENTS & REVIEW MODES (Go2Asia)
# ---------------------------

## Cursor должен всегда учитывать сводный файл ревью-режимов:
## docs/ai/roles/reviewers_overview.md

reviewers:
  enabled: true
  description: >
    В Go2Asia используется расширенная система ревью через субагентов:
    • Архитектурный ревьюер  
    • Ревьюер требований (ТЗ)  
    • Ревьюер планов разработки  
    • Ревьюер кода  
    Они описаны в docs/ai/roles/reviewers_overview.md и должны автоматически запускаться,
    когда это требуется пайплайном.

  files:
    - "docs/ai/roles/reviewers_overview.md"
    - "docs/ai/workflows/review_pipeline.md"

  rules:
    # Запуск ревью архитектуры
    - trigger: "architecture_change"
      reviewer: "architectural-reviewer"
      description: "Запускается при изменениях архитектуры, схем, модульных границ."

    # Запуск ревью ТЗ (аналитика)
    - trigger: "requirements_defined"
      reviewer: "requirements-reviewer"
      description: "Запускается после работы ИИ-аналитика и перед началом реализации."

    # Запуск ревью планов разработки
    - trigger: "plan_created"
      reviewer: "plan-reviewer"
      description: "Запускается после формирования плана новой Фазы/Этапа."

    # Запуск ревью кода
    - trigger: "code_ready_for_review"
      reviewer: "code-reviewer"
      description: "Запускается перед финальной интеграцией изменений в репозиторий."

  enforce:
    - "Всегда сверяться с файлом reviewers_overview.md"
    - "Следовать пайплайну ревью в docs/ai/workflows/review_pipeline.md"
    - "Не пропускать обязательные ревью-этапы"

# ===========================
# БЛОКИРОВКА ИНФРАСТРУКТУРЫ
# ===========================

Следующая инфраструктура уже настроена и НЕ ДОЛЖНА быть перенастроена:
- Netlify (хостинг и развертывание фронтенда)
- Cloudflare (DNS, Workers, R2)
- Neon PostgreSQL (существующая схема + демонстрационные данные)
- Clerk (аутентификация)
- GitHub Actions (развертывание бэкенда)

Правила:
- НЕ предлагайте альтернативы
- НЕ перенастраивайте эти сервисы
- НЕ создавайте для них руководства по настройке
- Архитектура должна адаптироваться к данной среде, а не наоборот
