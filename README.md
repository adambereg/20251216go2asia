# Go2Asia — мультиплатформенная экосистема для путешественников, экспатов и локальных бизнесов ЮВА

Добро пожаловать в монорепозиторий Go2Asia.  
Проект разрабатывается **с нуля**, с применением мультиагентной архитектуры (AI-орchestrator, аналитик, архитектор, dev-команда), строгих технических правил и модульной архитектуры.

Этот `README.md` — краткий обзор ключевых элементов репозитория.  
Полная документация находится в `docs/`.

---

# 🧱 Архитектура монорепозитория

Проект использует **PNPM Workspaces** и (опционально) Turborepo.

```
apps/             → фронтенд приложения (Next.js PWA Shell)
services/         → микросервисы (Cloudflare Workers)
packages/         → общие пакеты (UI Kit, SDK, types, utils)
docs/             → документация (архитектура, AI, контент)
design-system/    → read-only референс Bolt.New (компоненты, токены)
scripts/          → вспомогательные утилиты
tests/            → e2e тесты
prototypes/       → прототипы Bolt.New (read-only)
```

Файл `pnpm-workspace.yaml` подключает все эти директории в единый workspace.

---

# 🤖 Мультиагентная разработка

Go2Asia разрабатывается под управлением набора ИИ-агентов:

- Оркестратор  
- ИИ-аналитик  
- ИИ-архитектор  
- ИИ-планировщик  
- ИИ-фронтенд-разработчик  
- ИИ-бэкенд-разработчик  
- ИИ-DevOps  
- ИИ-QA  
- ИИ-специалист по безопасности  
- ИИ-технический писатель  

Все их роли, инструкции, рабочие процессы и ADR находятся здесь:

```
docs/ai/
```

Особенно важно:

- `docs/ai/context_map_for_cursor.md`
- `docs/ai/workflows/*.md`
- `docs/ai/roles/*.md`
- `docs/ai/decisions/*.md`

Cursor использует этот раздел как основу навигации.

---

# 📚 Документация

Полная документация находится в директории:

```
docs/
```

Стартовые файлы:

- `docs/README.md` — карта документации  
- `docs/overview/go2asia_overview.md` — концепция проекта  
- `docs/overview/go2asia_architecture.md` — общая архитектура  
- `docs/architecture/*` — FE/BE/API архитектура  
- `docs/modules/*` — описание модулей (Atlas, Pulse, Blog, Guru, Quest, Space, Connect и др.)

---

# 📦 Workspaces

Корневой workspace управляется PNPM:

```
pnpm-workspace.yaml
```

Подключены:

```
apps/*
services/*
packages/*
docs
design-system
scripts
tests
prototypes
```

---

# 🎨 Дизайн-система

Единственный источник UI-компонентов:

```
packages/ui/
```

Папка:

```
design-system/
```

является **READ-ONLY**, используется только как эталон Bolt.New.

Все правила описаны в `.cursor-rules`.

---

# 🚀 Старт разработки

1. Установить зависимости:

```
pnpm install
```

2. Запустить фронтенд (как только появится первая PWA Shell):

```
pnpm --filter go2asia-pwa-shell dev
```

3. Запустить микросервис (пример):

```
pnpm --filter api-gateway dev
```

4. Перед началом любой разработки:

- Изучить соответствующие документы в `docs/`
- Проверить ADR
- Уточнить модуль/слой, который затрагивается
- Следовать инструкциям `.cursor-rules`

---

# 🧭 Где начать разработку?

Стартовая точка — **PWA Shell**:

```
apps/go2asia-pwa-shell/
```

И базовый микросервис:

```
services/api-gateway/
```

---

# 🛡 Правила разработки

Фундаментальные принципы:

- **OpenAPI-first** — все типы и SDK генерируются из OpenAPI  
- **UI Kit-first** — компоненты только из `packages/ui`  
- **Cloudflare-first** — backend = Workers  
- **Документация — источник правды**  
- **ADR — финальный арбитр архитектурных решений**

Все правила формализированы в:

```
.cursor-rules
docs/decisions/*
docs/ai/*
```

---

# 🙌 Добро пожаловать в чистый старт Go2Asia

Проект начинается с нуля — это экспериментальная, но строго управляемая мультиагентная разработка.  
Каждый модуль, микросервис и документ будет создаваться по единым правилам, без ошибок прошлого цикла.

Если вы человек — следуйте документации.  
Если вы ИИ-агент — следуйте `.cursor-rules` и `docs/ai/`.

---

**Go2Asia — архитектура будущего, написанная людьми и AI вместе.**
