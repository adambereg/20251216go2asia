# Design System Go2Asia (Документация и Референсы)

> ⚠️ **ВНИМАНИЕ**: Это READ-ONLY директория для документации и референсов.
> 
> **Рабочие компоненты находятся в `packages/ui/`**
> 
> **НЕ редактируйте компоненты в `ui-components/`!**

---

## 📚 Назначение этой директории

Эта директория содержит:

1. **Документацию дизайн-системы** (`DESIGN_SYSTEM.md`, `DESIGN_TOKENS_GUIDE.md`)
2. **Дизайн-токены** (`design-tokens.json`) — справочник значений
3. **Конфигурацию Tailwind** (`tailwind.config.full.js`) — референс конфигурации
4. **Архив прототипов** (`ui-components/`) — исторические версии компонентов

## 🎯 Рабочий источник компонентов

**Все компоненты находятся в `packages/ui/`:**

```tsx
// ✅ Правильно
import { Button, Card, ModuleTile } from '@go2asia/ui';

// ❌ НЕПРАВИЛЬНО
import { Button } from '../../design-system/ui-components/Button';
```

## 📖 Использование

### Для чтения документации:
- `DESIGN_SYSTEM.md` — полное описание дизайн-системы
- `DESIGN_TOKENS_GUIDE.md` — руководство по дизайн-токенам

### Для референса значений:
- `design-tokens.json` — все дизайн-токены в JSON формате
- `tailwind.config.full.js` — полная конфигурация Tailwind

### Для референса компонентов:
- `ui-components/` — архив прототипов (только для чтения)

## 🔧 Изменение компонентов

**Все изменения компонентов делаются в `packages/ui/`:**

1. Редактировать: `packages/ui/src/components/ComponentName/`
2. Обновить Storybook: `packages/ui/src/components/ComponentName/ComponentName.stories.tsx`
3. НЕ редактировать: `design-system/ui-components/ComponentName.tsx`

## 📚 Связанные документы

- [Style Guide](../../docs/overview/go2asia_uiux_style_guide.md) — основной Style Guide
- [Component Library SSOT](../../docs/frontend/COMPONENT_LIBRARY_SOURCE_OF_TRUTH.md) — правила работы с компонентами
- [Project Structure](../../docs/PROJECT_STRUCTURE.md) — структура проекта

---

**Версия:** 1.0  
**Статус:** READ-ONLY документация  
**Рабочий код:** `packages/ui/`

