# Структура UI фронтенда Go2Asia

> **Документация структуры пользовательского интерфейса экосистемы Go2Asia**

**Версия:** 1.0  
**Дата:** 2025-01-14  
**Статус:** Архивная версия для капсулы

---

## 📋 Содержание

1. [Общая архитектура](#общая-архитектура)
2. [Структура страниц](#структура-страниц)
3. [Структура компонентов](#структура-компонентов)
4. [Роутинг](#роутинг)
5. [Layout система](#layout-система)
6. [Компоненты App Shell](#компоненты-app-shell)

---

## 🏗️ Общая архитектура

### Технологический стек

- **Framework:** Next.js 15 (App Router)
- **UI Library:** React 18
- **Styling:** Tailwind CSS
- **Type System:** TypeScript
- **State Management:** React Query (@tanstack/react-query)
- **Authentication:** Clerk
- **Maps:** Leaflet + React Leaflet
- **Icons:** Lucide React

### Архитектурные принципы

1. **Server Components по умолчанию** - используем Server Components для SEO и производительности
2. **Client Components для интерактивности** - только когда нужна интерактивность
3. **Модульная структура** - каждый модуль изолирован в своей папке
4. **Переиспользуемые компоненты** - общие компоненты в `components/`
5. **Типизация везде** - все компоненты и данные типизированы

---

## 📄 Структура страниц

### Расположение

Все страницы находятся в `apps/go2asia-pwa-shell/app/`

### Группировка роутов

#### `(public)/` - Публичные страницы (SSR/SSG)

Публичные страницы используют Server Components для SEO:

- **Atlas** (`/atlas`) - Энциклопедия мест
- **Blog** (`/blog`) - Статьи и гайды
- **Pulse** (`/pulse`) - События и афиша
- **Quest** (`/quest`) - Квесты и миссии
- **RF** (`/rf`) - Каталог партнёров
- **Rielt** (`/rielt`) - Недвижимость
- **Space** (`/space`) - Социальная сеть
- **Guru** (`/guru`) - Объекты на карте
- **About** (`/about`) - О проекте
- **Help** (`/help`) - Помощь

#### `(authenticated)/` - Защищённые страницы (SPA)

Требуют аутентификации через Clerk:

- **Connect** (`/connect`) - Кошелёк и достижения
- **Profile** (`/profile`) - Профиль пользователя
- **Settings** (`/settings`) - Настройки
- **Partner** (`/partner`) - Партнёр
- **RF Merchant** (`/rf/merchant/*`) - Панель партнёра
- **RF PRO** (`/rf/pro/*`) - Панель PRO пользователя

#### `(auth)/` - Аутентификация

- **Sign In** (`/sign-in`) - Вход
- **Sign Up** (`/sign-up`) - Регистрация

### Структура страницы

```typescript
// Пример: app/(public)/atlas/page.tsx
import type { Metadata } from 'next';
import { AtlasHomeClient } from './AtlasHomeClient';

export const metadata: Metadata = {
  title: 'Atlas Asia - Go2Asia',
  description: '...',
};

export default function AtlasPage() {
  return <AtlasHomeClient />;
}
```

---

## 🧩 Структура компонентов

### Расположение

Все компоненты находятся в `apps/go2asia-pwa-shell/components/`

### Организация

```
components/
├── app-shell/          # Оболочка приложения
│   ├── TopAppBar.tsx   # Верхняя панель
│   ├── BottomNav.tsx    # Нижняя навигация
│   ├── SideDrawer.tsx  # Боковое меню
│   └── AppShellProvider.tsx
├── [module]/           # Компоненты модуля
│   ├── components/     # UI компоненты модуля
│   ├── utils/          # Утилиты модуля
│   ├── types.ts        # Типы модуля
│   ├── mockData.ts     # Моки данных (только для разработки!)
│   └── index.ts        # Экспорты
├── landing/            # Компоненты главной страницы
├── modules/            # Общие компоненты модулей
├── skeletons/          # Skeleton loaders
└── auth/               # Компоненты аутентификации
```

### Правила именования

- **Компоненты:** PascalCase (`CountryCard.tsx`)
- **Утилиты:** camelCase (`filterEvents.ts`)
- **Типы:** PascalCase (`Event`, `EventFilters`)
- **Константы:** UPPER_SNAKE_CASE (`QUEST_TYPE_COLORS`)

---

## 🗺️ Роутинг

### Next.js App Router

Используется файловая система роутинга Next.js 15:

```
app/
├── (public)/
│   ├── atlas/
│   │   ├── page.tsx              # /atlas
│   │   ├── countries/
│   │   │   ├── page.tsx          # /atlas/countries
│   │   │   └── [id]/
│   │   │       ├── page.tsx      # /atlas/countries/[id]
│   │   │       ├── visas/
│   │   │       │   └── page.tsx  # /atlas/countries/[id]/visas
│   │   │       └── ...
│   │   └── ...
│   └── ...
└── ...
```

### Динамические роуты

- `[id]` - динамический параметр
- `[[...slug]]` - catch-all роут (для Clerk)

### Layout файлы

- `layout.tsx` - Layout для группы роутов
- Наследуется вниз по дереву роутов

---

## 📐 Layout система

### Корневой Layout

`app/layout.tsx` - обёртка всего приложения:

```typescript
export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        <Providers>
          <AppShell>
            {children}
          </AppShell>
        </Providers>
      </body>
    </html>
  );
}
```

### Групповые Layouts

- `(public)/layout.tsx` - Layout для публичных страниц
- `(authenticated)/layout.tsx` - Layout для защищённых страниц
- `[module]/layout.tsx` - Layout конкретного модуля

### ModuleLayout компонент

Переиспользуемый компонент для модулей:

```typescript
<ModuleLayout
  hero={{
    icon: MapIcon,
    title: "Atlas Asia",
    description: "Энциклопедия мест"
  }}
>
  {children}
</ModuleLayout>
```

---

## 🎨 Компоненты App Shell

### TopAppBar

Верхняя панель навигации:

- Логотип
- Поиск
- Уведомления
- Профиль пользователя

**Расположение:** `components/app-shell/TopAppBar.tsx`

### BottomNav

Нижняя навигация (мобильная):

- 5 основных модулей
- Адаптивная (скрывается на десктопе)

**Расположение:** `components/app-shell/BottomNav.tsx`

### SideDrawer

Боковое меню (планшет/десктоп):

- Навигация по модулям
- Профиль пользователя
- Настройки

**Расположение:** `components/app-shell/SideDrawer.tsx`

---

## 📦 Модули экосистемы

### 1. Atlas Asia

**Роуты:** `/atlas/*`

**Страницы:**
- Главная (`/atlas`)
- Страны (`/atlas/countries/[id]`) - 13 подстраниц
- Города (`/atlas/cities/[id]`) - 12 подстраниц
- Места (`/atlas/places/[id]`) - 9 подстраниц
- Гайды (`/atlas/guides/[id]`) - 6 подстраниц
- Темы (`/atlas/themes/[id]`) - 7 подстраниц
- Хабы (`/atlas/hubs/[slug]`)
- Инструменты (`/atlas/tools/*`)

**Компоненты:** `components/atlas/`

### 2. Blog Asia

**Роуты:** `/blog/*`

**Страницы:**
- Главная (`/blog`)
- Статья (`/blog/[slug]`)
- Категория (`/blog/category/[id]`)
- Тема (`/blog/theme/[id]`)

**Компоненты:** `components/blog/`

### 3. Pulse Asia

**Роуты:** `/pulse/*`

**Страницы:**
- Главная (`/pulse`)
- Событие (`/pulse/[id]`)

**Компоненты:** `components/pulse/`

### 4. Quest Asia

**Роуты:** `/quest/*`

**Страницы:**
- Главная (`/quest`)
- Квест (`/quest/[id]`)
- Запуск (`/quest/[id]/run`)
- Завершение (`/quest/[id]/complete`)
- Мои квесты (`/quest/my`)
- Лидерборд (`/quest/leaderboard`)

**Компоненты:** `components/quest/`

### 5. Russian Friendly

**Роуты:** `/rf/*`

**Страницы:**
- Каталог (`/rf`)
- Партнёр (`/rf/[id]`)
- Ваучеры (`/rf/vouchers`)
- Merchant (`/rf/merchant/*`) - 6 страниц
- PRO (`/rf/pro/*`) - 5 страниц

**Компоненты:** `components/rf/`

### 6. Rielt Asia

**Роуты:** `/rielt/*`

**Страницы:**
- Главная (`/rielt`)
- Объявление (`/rielt/listings/[id]`)
- Поиск (`/rielt/search`)

**Компоненты:** `components/rielt/`

### 7. Space Asia

**Роуты:** `/space/*`

**Страницы:**
- Главная (`/space`)
- Баланс (`/space/balance`)
- Сообщество (`/space/community`)
- Лента (`/space/community/feed`)
- Посты (`/space/posts`)
- Квесты (`/space/quests`)
- Рефералы (`/space/referrals`)
- NFT (`/space/nft`)
- Ваучеры (`/space/vouchers`)
- Настройки (`/space/settings`)

**Компоненты:** `components/space/`

### 8. Guru Asia

**Роуты:** `/guru/*`

**Страницы:**
- Главная (`/guru`) - карта и список объектов

**Компоненты:** `components/guru/`

### 9. Connect Asia

**Роуты:** `/connect/*` (защищённый)

**Страницы:**
- Дашборд (`/connect`)
- Кошелёк (`/connect/wallet`)
- Рефералы (`/connect/referrals`)
- Миссии (`/connect/missions`)
- Уровни (`/connect/levels`)
- Аналитика (`/connect/analytics`)

**Компоненты:** `components/connect/`

---

## 🎯 Паттерны использования

### Server Component → Client Component

```typescript
// app/(public)/atlas/page.tsx (Server Component)
import { AtlasHomeClient } from './AtlasHomeClient';

export default function AtlasPage() {
  return <AtlasHomeClient />;
}

// app/(public)/atlas/AtlasHomeClient.tsx (Client Component)
'use client';

export function AtlasHomeClient() {
  // Интерактивная логика
}
```

### Использование типов

```typescript
// components/[module]/types.ts
export interface Event {
  id: string;
  title: string;
  // ...
}

// components/[module]/components/EventCard.tsx
import { Event } from '../types';

interface EventCardProps {
  event: Event;
}
```

### Моки данных (только для разработки!)

```typescript
// components/[module]/mockData.ts
export const mockEvents: Event[] = [
  // ...
];

// В компоненте (заменить на реальный API!)
const events = mockEvents; // ❌ Только для разработки!
```

---

## 📚 Связанные документы

- [design_system.md](./design_system.md) - Дизайн-система
- [atlas_ui.md](./atlas_ui.md) - UI модуля Atlas
- [pulse_ui.md](./pulse_ui.md) - UI модуля Pulse
- [blog_ui.md](./blog_ui.md) - UI модуля Blog
- [quest_ui.md](./quest_ui.md) - UI модуля Quest
- [rf_ui.md](./rf_ui.md) - UI модуля Russian Friendly
- [rielt_ui.md](./rielt_ui.md) - UI модуля Rielt
- [space_ui.md](./space_ui.md) - UI модуля Space
- [guru_ui.md](./guru_ui.md) - UI модуля Guru
- [connect_ui.md](./connect_ui.md) - UI модуля Connect

---

**Версия:** 1.0  
**Дата:** 2025-01-14  
**Статус:** Архивная версия для капсулы

