{
  "name": "@go2asia/points-service",
  "version": "0.1.0",
  "private": true,
  "description": "Points Service for Go2Asia MVP (Cloudflare Worker)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --env staging",
    "deploy": "wrangler deploy --env staging",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@go2asia/db": "workspace:*",
    "@go2asia/logger": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "@go2asia/config": "workspace:*",
    "typescript": "^5.3.3",
    "vitest": "^1.0.0",
    "wrangler": "^3.19.0"
  }
}
