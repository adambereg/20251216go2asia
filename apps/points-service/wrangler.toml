name = "go2asia-points-service"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[env.production]
name = "go2asia-points-service"

[env.staging]
name = "go2asia-points-service-staging"

[env.staging.vars]
ENVIRONMENT = "staging"
POINTS_ENABLE_SPENDABILITY_SHADOW_COMPARE = "true"
POINTS_ENABLE_SPENDABILITY_SHADOW_DIAGNOSTICS = "true"
POINTS_ENABLE_SPENDABILITY_DURABLE_EXPORT = "true"
ECONOMY_PRODUCER_FIRST_LOGIN_ENABLED = "true"
ECONOMY_PRODUCER_QUEST_COMPLETED_ENABLED = "true"
ECONOMY_PRODUCER_EVENT_REGISTRATION_ENABLED = "true"
ECONOMY_PRODUCER_RF_VOUCHER_CLAIM_SPEND_ENABLED = "true"
ECONOMY_PRODUCER_RF_VOUCHER_CLAIM_SPEND_COMPENSATION_ENABLED = "true"

# Runtime vars:
# - ENVIRONMENT, staging spendability flags, and Stage 11.2 internal-beta producer flags are pinned above so dashboard-only edits do not drift on deploy.
# - VERSION is injected by CI/CD as the git sha.
#
# Required vars/secrets/bindings for Milestone 3:
# - DATABASE_URL (var/secret)
# - SERVICE_JWT_SECRET (secret, shared with api-gateway for gateway-origin auth and service-to-service auth)
#
# Optional config (vars):
# - POINTS_VELOCITY_CAP
# - POINTS_VELOCITY_WINDOW_SECONDS
