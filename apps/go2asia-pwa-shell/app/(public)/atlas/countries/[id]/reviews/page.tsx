import { AtlasTabContent } from '@/modules/atlas/components/AtlasTabContent';

export default function CountryReviewsPage() {
  return <AtlasTabContent entityType="country" tabKey="reviews" title="Отзывы экспатов" />;
}

