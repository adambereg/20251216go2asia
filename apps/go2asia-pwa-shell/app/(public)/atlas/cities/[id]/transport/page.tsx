import { AtlasTabContent } from '@/modules/atlas/components/AtlasTabContent';

export default function CityTransportPage() {
  return <AtlasTabContent entityType="city" tabKey="transport" title="Транспорт" />;
}

