{
  "name": "@go2asia/atlas-taxonomy",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Atlas taxonomy (categories/tags) - pure types and functions",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.0.0"
  }
}

