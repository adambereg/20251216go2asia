{
  "name": "@go2asia/identity-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared identity semantics contracts and golden fixtures",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:ci": "vitest run --config ./vitest.config.ts"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.1",
    "typescript": "^5.3.3",
    "vitest": "^1.0.0"
  }
}
