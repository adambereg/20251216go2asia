{
  "name": "@go2asia/vip-entitlement-runtime-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Non-authoritative VIP entitlement runtime contract labels and shared semantic foundations",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "pnpm build && node --test \"test/*.test.mjs\""
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
