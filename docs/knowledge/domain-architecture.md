# 🌐 Domain Architecture — структура доменов экосистемы Go2Asia

## 🎯 Цель документа
Определить итоговую логику распределения доменов между пользовательской (PWA) и служебной (API) зонами экосистемы **Go2Asia**.  
Документ обеспечивает единый подход для настройки Clerk SSO, маршрутизации, CI/CD, CORS и безопасности.

---

## 🧭 Общий принцип

Go2Asia использует **гибридную доменную архитектуру**:

- **Фронтенды (Bolt.New)** работают **под одним основным доменом `go2asia.space`** — все модули размещены как подпапки (`/atlas`, `/pulse`, `/blog` и т.д.).  
  Это обеспечивает единые cookies, прозрачную авторизацию через Clerk и общий PWA-контекст.

- **Бэкенды (Cursor)** работают **на поддоменах** (`api.go2asia.space`, `auth.go2asia.space`, `token.go2asia.space` и др.),  
  обеспечивая изоляцию API, безопасность, масштабируемость и независимые деплои.

---

## 🟢 Клиентская зона (Frontend / PWA)

### Основные принципы
- Один **PWA App Shell** (`go2asia.space`) управляет маршрутизацией и загрузкой микрофронтендов.
- Все cookie Clerk распространяются на весь домен (`cookie domain = go2asia.space`).
- Общий Service Worker обслуживает кэширование и офлайн-доступ для всех подпапок.

### Структура пользовательских маршрутов

| Модуль | URL-путь | Назначение |
|--------|-----------|------------|
| **App Shell (главная)** | `/` | Точка входа, лендинг, навигация по экосистеме |
| **Atlas Asia** | `/atlas/*` | Страны, города, места |
| **Pulse Asia** | `/pulse/*` | События и мероприятия |
| **Blog Asia** | `/blog/*` | Новости и статьи |
| **Guru Asia** | `/guru/*` | AI-гид и интерактивная карта |
| **Space Asia** | `/space/*` | Социальная сеть и лента |
| **Quest Asia** | `/quest/*` | Квесты, миссии и челленджи |
| **Russian Friendly Asia** | `/rf/*` | Каталог партнёрских бизнесов |
| **Connect Asia** | `/connect/*` | Реферальные связи и дерево взаимодействий |
| **Profile / Wallet** | `/profile/*` | Личный кабинет, NFT, балансы, награды |
| **Help / Docs** | `/help/*` | Справка, FAQ, правила |

📍 **Все эти модули используют общий Clerk SSO** и взаимодействуют с API через HTTPS-запросы к служебным поддоменам.

---

## 🟠 Служебная зона (Backend / API)

### Основные принципы
- Каждая логическая подсистема Go2Asia имеет свой отдельный поддомен.  
- Все API доступны через защищённый HTTPS и управляются централизованно через **API Gateway**.  
- Взаимодействие между микросервисами идёт по JWT, очередям (Redis, Tasks) и webhooks.

### Структура поддоменов и сервисов

| Сервис | Поддомен | Назначение |
|--------|-----------|------------|
| **API Gateway** | `api.go2asia.space` | Единая точка входа для фронтов; маршрутизация к микросервисам |
| **Auth Service** | `auth.go2asia.space` | Авторизация, OAuth, Clerk Webhooks, JWT, роли |
| **Token Service** | `token.go2asia.space` | Учёт Points и off-chain G2A, балансы |
| **Blockchain Gateway** | `chain.go2asia.space` | Взаимодействие с TON, mint/burn NFT, мост G2A |
| **Referral / Connect API** | `connect.go2asia.space` | Учёт реферальных связей и вознаграждений |
| **RF Merchant API** | `rf-api.go2asia.space` | Работа с бизнес-партнёрами Russian Friendly |
| **Content API** | `content.go2asia.space` | Хранение и выдача контента Atlas, Pulse, Blog |
| **Quest API** | `quest-api.go2asia.space` | Управление квестами и миссиями PRO |
| **Anti-Abuse & Risk Control** | `risk.go2asia.space` | Антифрод, лимиты, аудит транзакций |
| **Analytics Service** | `stats.go2asia.space` | Метрики, отчёты, статистика, агрегаты |

📍 Эти сервисы **не используют cookie**, а работают по **Bearer JWT-токенам**.  
Каждый микросервис имеет свой CI/CD и деплоится независимо (Cloudflare Workers, Yandex Cloud Functions, Netlify Functions и т.п.).

---

## 🔒 Безопасность и CORS

| Зона | Метод авторизации | Cookie / Token | Примечание |
|------|--------------------|----------------|-------------|
| Клиентская зона (PWA) | Clerk SSO | Cookie (`go2asia.space`) | Shared сессия |
| Служебная зона (API) | JWT / Bearer | Token Header | Без cookie |
| Внутренние сервисы | Service-to-Service JWT | Internal Key | Без участия пользователя |

Все запросы с фронтендов к API проходят через HTTPS и валидируются по JWT и CORS-политике.  
Cookie никогда не пересылаются на поддомены.

---

## 🧱 CI/CD и DevOps

- **Frontend (Bolt.New):** деплой на Netlify, все модули через App Shell.  
- **Backend (Cursor):** деплой микросервисов на Cloudflare / Yandex Cloud / Netlify Functions.  
- **API Gateway:** маршрутизация по поддоменам.  
- **Clerk:** авторизация на уровне фронта (`go2asia.space`) с прозрачным SSO.

---

## 🔗 Взаимосвязь доменов (схема)

                     ┌────────────────────────┐
                     │ go2asia.space (App Shell)│
                     └──────────┬───────────────┘
┌────────────────────────────────┴────────────────────────────────┐
│ PWA Модули (подпапки фронтенда) │
│ atlas/ pulse/ blog/ guru/ space/ quest/ rf/ connect/ ... │
└────────────────────────────────┬────────────────────────────────┘
│ REST / HTTPS (JWT)
┌───────────┴────────────────┐
│ API Subdomains │
├────────────────────────────┤
│ api.go2asia.space │
│ auth.go2asia.space │
│ token.go2asia.space │
│ chain.go2asia.space │
│ connect.go2asia.space │
│ rf-api.go2asia.space │
│ content.go2asia.space │
│ risk.go2asia.space │
│ stats.go2asia.space │
└────────────────────────────┘


---

## ✅ Резюме

- Пользовательские фронтенды размещаются **в подпапках основного домена** `go2asia.space`.  
- Все служебные микросервисы размещаются **на поддоменах** (`api`, `auth`, `token`, `chain` и т.д.).  
- Такой подход обеспечивает:
  - прозрачную авторизацию (Clerk cookie shared на фронте);
  - безопасность (JWT и CORS между фронтами и API);
  - масштабируемость (независимый деплой бэкендов);
  - простоту обслуживания (единый домен для пользователей, отдельные зоны для сервисов).

---

📘 *Документ создан как основа для настройки DNS, CI/CD, Clerk и PWA App Shell.  
Он должен использоваться при любом изменении доменной структуры Go2Asia.*

---

## 🌍 DNS и SSL-рекомендации

### 1. DNS-управление

Go2Asia использует комбинированную схему:  
- основной домен `go2asia.space` — управляется через **Cloudflare** (DNS + CDN + SSL);  
- фронтенд (PWA) деплоится на **Netlify**;  
- микросервисы (API) — на **Cloudflare Workers**.

#### Основные записи DNS

| Запись | Тип | Цель / Значение | Комментарий |
|--------|-----|-----------------|--------------|
| `go2asia.space` | A / CNAME | `Netlify` (или alias Netlify load balancer) | Главный фронтенд (App Shell) |
| `www` | CNAME | `go2asia.space` | Перенаправление на основной домен |
| `api` | CNAME | `workers.cloudflare.com` | Центральный API Gateway |
| `auth` | CNAME | `workers.cloudflare.com` | Auth Service |
| `token` | CNAME | `workers.cloudflare.com` | Token Service |
| `chain` | CNAME | `workers.cloudflare.com` | Blockchain Gateway |
| `connect` | CNAME | `workers.cloudflare.com` | Connect/Referral API |
| `rf-api` | CNAME | `workers.cloudflare.com` | Russian Friendly Merchant API |
| `content` | CNAME | `workers.cloudflare.com` | Content API |
| `quest-api` | CNAME | `workers.cloudflare.com` | Quest API |
| `risk` | CNAME | `workers.cloudflare.com` | Anti-Abuse & Risk Control |
| `stats` | CNAME | `workers.cloudflare.com` | Analytics Service |
| `_acme-challenge` | TXT | Динамический ключ верификации SSL | Используется при выпуске сертификатов Let’s Encrypt |
| `spf`, `dmarc`, `dkim` | TXT | Настройки почты (если используется Postbox / Yandex 360) | Для писем и уведомлений |

> ⚙️ Все записи рекомендуется держать под управлением Cloudflare DNS, чтобы использовать автоматическое SSL и проксирование (🟠 оранжевая туча = включён прокси).

---

### 2. SSL / HTTPS

#### Основной принцип
- Вся экосистема **работает только по HTTPS**.  
- Cloudflare управляет **универсальным wildcard-сертификатом `*.go2asia.space`**, который покрывает все поддомены.

#### Сертификаты
| Зона | Сертификат | Управление |
|------|-------------|-------------|
| `go2asia.space` (фронтенд) | Автоматический SSL от Netlify или Cloudflare | Поддерживается автоматически при CNAME → Netlify |
| Все поддомены (`*.go2asia.space`) | Wildcard SSL от Cloudflare | Один сертификат на все поддомены |
| Служебные функции (Yandex Cloud Functions) | Автоматическое HTTPS от Yandex Cloud | Использует собственные managed certificates |
| API Gateway | Cloudflare Edge SSL | Терминация TLS на уровне Workers |

#### Особенности
- Нет необходимости выпускать отдельные сертификаты для каждого микросервиса.  
- В случае нестандартных интеграций (например, внешние Webhook URL для Clerk, Telegram, TON Gateway) — Cloudflare автоматически выпускает **Origin Certificates**.  
- Для dev/test окружений можно использовать **Cloudflare Tunnel** или **Netlify Preview**.

---

### 3. Рекомендации по конфигурации

- Включить **Always Use HTTPS** и **Automatic HTTPS Rewrites** в Cloudflare.  
- Включить **Full (Strict)** режим SSL для всех поддоменов.  
- Активировать **HSTS (HTTP Strict Transport Security)**:  
  max-age=31536000; includeSubDomains; preload

- Проверить, что CORS разрешён только с домена `https://go2asia.space`.
- Для Auth и API Gateway настроить `Access-Control-Allow-Origin` = `https://go2asia.space`.

---

### 4. Верификация домена (Clerk, Netlify, Google, etc.)

| Сервис | Тип записи | Пример | Назначение |
|---------|-------------|--------|------------|
| **Clerk Auth** | TXT | `_clerk-verification=XXXX` | Подтверждение домена для OAuth и SSO |
| **Netlify** | TXT | `netlify-domain-verification=XXXX` | Проверка владения доменом |
| **Google Search Console / GA4** | TXT | `google-site-verification=XXXX` | SEO и аналитика |
| **Yandex Cloud** | TXT | `yandex-verification=XXXX` | Подтверждение владения при подключении функций |
| **Mail / Postbox** | SPF / DKIM / DMARC | `v=spf1 include:_spf.yandex.net ~all` | Почтовая аутентификация |

---

### 5. Dev / Test / Prod окружения

| Среда | Домен / Поддомен | Назначение |
|--------|------------------|------------|
| **Dev** | `dev.go2asia.space` | тестовая сборка фронта (Netlify Preview) |
| **Staging** | `staging.go2asia.space` | предрелизная сборка с подключением sandbox API |
| **Prod** | `go2asia.space` | основная продуктивная среда |
| **Test API** | `api-staging.go2asia.space` | sandbox API Gateway |
| **Test Auth** | `auth-staging.go2asia.space` | тестовые OAuth и Clerk Webhooks |

Все окружения должны использовать одинаковую схему поддоменов и SSL, чтобы избежать CORS-проблем.

---

### ✅ Итого

- Все фронтенды — под основным доменом `go2asia.space`.  
- Все API и системные сервисы — под своими поддоменами.  
- SSL и DNS управляются через Cloudflare (Wildcard `*.go2asia.space`).  
- HTTPS включён на всех уровнях, HSTS активирован.  
- Все cookie (Clerk SSO) работают только внутри основного домена.

> 📘 Этот раздел является частью DevOps-руководства и используется при настройке DNS, CI/CD и SSL для всех окружений Go2Asia.
