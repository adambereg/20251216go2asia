openapi: 3.1.0

info:
  title: Go2Asia RF Service API
  version: 0.1.0
  description: >
    Downstream API contract for RF runtime baseline (Step 11).
    Covers implemented partner/offer/voucher/pro-link practical surfaces only.
    Planned/extended RF API surface is tracked in docs/architecture/rf/rf_openapi_outline_v1.md
    and is not implied as implemented by this file.
    Note: root health paths (/health, /version, /ready) are intentionally excluded from
    docs/openapi/openapi.bundle.yaml by bundle rules (only /v1/* and /internal/*).

servers:
  - url: https://staging.api.go2asia.space
    description: Staging API Gateway
  - url: https://{workerHost}
    description: Direct service URL (workers.dev / local)
    variables:
      workerHost:
        default: rf-service.example

tags:
  - name: health
  - name: rf
    description: >
      Runtime-implemented RF baseline for downstream consumption.
      Use this tag contract for integration; treat outline-only endpoints as planned.
    x-go2asia-rf:
      contractClass: runtime-implemented
      plannedSurfaceDoc: docs/architecture/rf/rf_openapi_outline_v1.md
      stableRefPolicy:
        partnerId:
          wireFormat: string
          opaque: true
          assignedBy: rf-service
        offerId:
          wireFormat: string
          opaque: true
          assignedBy: rf-service
        voucherId:
          wireFormat: string
          opaque: true
          assignedBy: rf-service
        proLinkId:
          wireFormat: string
          opaque: true
          assignedBy: rf-service
        branchId:
          presentInThisContract: false
          firstSlicePolicy: >
            Downstream first-slice integrations must not require branchId until
            branch runtime surface is explicitly added to this OpenAPI contract.
      consumerSeam:
        normativeHttpPaths:
          - /v1/rf/partners
          - /v1/rf/partners/{partnerId}
          - /v1/rf/offers
          - /v1/rf/offers/{offerId}
          - /v1/rf/rielt/listings/{listingId}/offers
          - /v1/rf/rielt/listings/{listingId}/offers/{offerId}/claim
          - /v1/rf/business/partners
          - /v1/rf/business/partners/{partnerId}/offers
          - /v1/rf/business/partners/{partnerId}/offers/{offerId}/activate
          - /v1/rf/offers/{offerId}/claim
          - /v1/rf/me/vouchers
          - /v1/rf/me/vouchers/summary
          - /v1/rf/business/partners/{partnerId}/vouchers/{voucherId}/redeem
          - /v1/rf/pro/links
          - /v1/rf/pro/links/{proLinkId}/accept
        explicitlyOutOfScopeInThisSpec:
          - /v1/rf/branches
          - /v1/rf/internal/*
          - moderation and verification workflow endpoints beyond current baseline

paths:
  /health:
    get:
      tags: [health]
      operationId: getRfHealth
      summary: Health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfHealthResponse"

  /version:
    get:
      tags: [health]
      operationId: getRfVersion
      summary: Version (alias of /health)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfHealthResponse"

  /ready:
    get:
      tags: [health]
      operationId: getRfReady
      summary: Readiness check
      responses:
        "200":
          description: Ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfReadyResponse"
        "503":
          description: Not ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfReadyResponse"

  /v1/rf/partners:
    get:
      tags: [rf]
      operationId: rfListPartners
      summary: List public partners
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfPartnerListResponse"

  /v1/rf/business/partners:
    post:
      tags: [rf]
      operationId: rfCreatePartner
      summary: Create partner (business owner flow)
      security:
        - GatewayAuth: []
      parameters:
        - $ref: "#/components/parameters/RfXGatewayAuth"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RfCreatePartnerRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfPartner"
        "400":
          $ref: "#/components/responses/RfValidationError"
        "401":
          $ref: "#/components/responses/RfUnauthorized"

  /v1/rf/partners/{partnerId}:
    get:
      tags: [rf]
      operationId: rfGetPartner
      summary: Get public partner
      parameters:
        - $ref: "#/components/parameters/RfPartnerId"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfPartner"
        "404":
          $ref: "#/components/responses/RfNotFound"

  /v1/rf/offers:
    get:
      tags: [rf]
      operationId: rfListOffers
      summary: List public active offers
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfOfferListResponse"

  /v1/rf/offers/{offerId}:
    get:
      tags: [rf]
      operationId: rfGetOffer
      summary: Get public offer
      parameters:
        - $ref: "#/components/parameters/RfOfferId"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfOffer"
        "404":
          $ref: "#/components/responses/RfNotFound"

  /v1/rf/rielt/listings/{listingId}/offers:
    get:
      tags: [rf]
      operationId: rfGetRieltListingOffers
      summary: Get RF offers mapped to a Rielt listing
      description: >
        Read-only RF-owned listing voucher context. Returns public active RF offers explicitly
        mapped to the Rielt listing. Does not claim or redeem vouchers.
      parameters:
        - $ref: "#/components/parameters/RfRieltListingId"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfRieltListingOfferContext"
        "404":
          $ref: "#/components/responses/RfNotFound"

  /v1/rf/rielt/listings/{listingId}/offers/{offerId}/claim:
    post:
      tags: [rf]
      operationId: rfClaimRieltListingVoucher
      summary: Claim listing-scoped voucher for mapped Rielt offer
      description: >
        Claims a voucher in RF for a specific Rielt listing context. RF validates that
        the offer is actively mapped to the listing, the offer is public and active,
        and the partner is active. This endpoint does not replace the partner-scope
        claim endpoint.
      security:
        - GatewayAuth: []
      parameters:
        - $ref: "#/components/parameters/RfXGatewayAuth"
        - $ref: "#/components/parameters/RfRieltListingId"
        - $ref: "#/components/parameters/RfOfferId"
        - $ref: "#/components/parameters/RfIdempotencyKey"
      responses:
        "200":
          description: Idempotent replay
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfClaimResponse"
        "201":
          description: Listing-scoped voucher created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfClaimResponse"
        "400":
          $ref: "#/components/responses/RfValidationError"
        "401":
          $ref: "#/components/responses/RfUnauthorized"
        "404":
          $ref: "#/components/responses/RfNotFound"
        "409":
          description: >
            Conflict, including RF_IDEMPOTENCY_KEY_CONTEXT_MISMATCH.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfErrorResponse"
        "429":
          $ref: "#/components/responses/RfRateLimited"
        "500":
          $ref: "#/components/responses/RfInternalError"
        "503":
          $ref: "#/components/responses/RfServiceUnavailable"

  /v1/rf/business/partners/{partnerId}/offers:
    post:
      tags: [rf]
      operationId: rfCreateOffer
      summary: Create draft offer for partner
      security:
        - GatewayAuth: []
      parameters:
        - $ref: "#/components/parameters/RfXGatewayAuth"
        - $ref: "#/components/parameters/RfPartnerId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RfCreateOfferRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfOffer"
        "400":
          $ref: "#/components/responses/RfValidationError"
        "401":
          $ref: "#/components/responses/RfUnauthorized"
        "403":
          $ref: "#/components/responses/RfForbidden"
        "404":
          $ref: "#/components/responses/RfNotFound"

  /v1/rf/business/partners/{partnerId}/offers/{offerId}/activate:
    post:
      tags: [rf]
      operationId: rfActivateOffer
      summary: Activate partner offer
      security:
        - GatewayAuth: []
      parameters:
        - $ref: "#/components/parameters/RfXGatewayAuth"
        - $ref: "#/components/parameters/RfPartnerId"
        - $ref: "#/components/parameters/RfOfferId"
      responses:
        "200":
          description: Activated or already active
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfOffer"
        "401":
          $ref: "#/components/responses/RfUnauthorized"
        "403":
          $ref: "#/components/responses/RfForbidden"
        "404":
          $ref: "#/components/responses/RfNotFound"
        "409":
          $ref: "#/components/responses/RfConflict"

  /v1/rf/offers/{offerId}/claim:
    post:
      tags: [rf]
      operationId: rfClaimVoucher
      summary: Claim voucher for active offer (idempotent by user+key)
      description: >
        Claim is allowed only for active public offers of active RF partners.
        Runtime status uses `claimed`; in product semantics this corresponds to an issued voucher.
      security:
        - GatewayAuth: []
      parameters:
        - $ref: "#/components/parameters/RfXGatewayAuth"
        - $ref: "#/components/parameters/RfOfferId"
        - $ref: "#/components/parameters/RfIdempotencyKey"
      responses:
        "200":
          description: Idempotent replay
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfClaimResponse"
        "201":
          description: Voucher created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfClaimResponse"
        "400":
          $ref: "#/components/responses/RfValidationError"
        "401":
          $ref: "#/components/responses/RfUnauthorized"
        "404":
          $ref: "#/components/responses/RfNotFound"
        "409":
          $ref: "#/components/responses/RfConflict"
        "429":
          $ref: "#/components/responses/RfRateLimited"
        "500":
          $ref: "#/components/responses/RfInternalError"
        "503":
          $ref: "#/components/responses/RfServiceUnavailable"

  /v1/rf/me/vouchers:
    get:
      tags: [rf]
      operationId: rfListMyVouchers
      summary: List current user vouchers
      security:
        - GatewayAuth: []
      parameters:
        - $ref: "#/components/parameters/RfXGatewayAuth"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfVoucherListResponse"
        "401":
          $ref: "#/components/responses/RfUnauthorized"

  /v1/rf/me/vouchers/summary:
    get:
      tags: [rf]
      operationId: rfGetMyVoucherSummary
      summary: Get current user voucher summary
      description: >
        Read-only RF-owned voucher summary for the authenticated user. Counts are based on
        RF runtime statuses; `claimed` is the active/issued product-semantic bucket.
      security:
        - GatewayAuth: []
      parameters:
        - $ref: "#/components/parameters/RfXGatewayAuth"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfVoucherSummary"
        "401":
          $ref: "#/components/responses/RfUnauthorized"

  /v1/rf/business/partners/{partnerId}/vouchers/{voucherId}/redeem:
    post:
      tags: [rf]
      operationId: rfRedeemVoucher
      summary: Redeem voucher by partner owner
      description: >
        Redeem is an owner-authorized partner-side flow. The authenticated actor must own
        the partner in path, and voucher relation (voucher.partnerId, voucher.offerId, offer.partnerId)
        must be consistent.
      security:
        - GatewayAuth: []
      parameters:
        - $ref: "#/components/parameters/RfXGatewayAuth"
        - $ref: "#/components/parameters/RfPartnerId"
        - $ref: "#/components/parameters/RfVoucherId"
      responses:
        "200":
          description: Redeem processed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfRedeemResponse"
        "401":
          $ref: "#/components/responses/RfUnauthorized"
        "403":
          $ref: "#/components/responses/RfForbidden"
        "404":
          $ref: "#/components/responses/RfNotFound"
        "409":
          $ref: "#/components/responses/RfConflict"
        "429":
          $ref: "#/components/responses/RfRateLimited"
        "503":
          $ref: "#/components/responses/RfServiceUnavailable"

  /v1/rf/pro/links:
    get:
      tags: [rf]
      operationId: rfListProLinks
      summary: List current user PRO links
      security:
        - GatewayAuth: []
      parameters:
        - $ref: "#/components/parameters/RfXGatewayAuth"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfProLinkListResponse"
        "401":
          $ref: "#/components/responses/RfUnauthorized"
    post:
      tags: [rf]
      operationId: rfCreateProLink
      summary: Create PRO link
      security:
        - GatewayAuth: []
      parameters:
        - $ref: "#/components/parameters/RfXGatewayAuth"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RfCreateProLinkRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfProLink"
        "400":
          $ref: "#/components/responses/RfValidationError"
        "401":
          $ref: "#/components/responses/RfUnauthorized"
        "404":
          $ref: "#/components/responses/RfNotFound"

  /v1/rf/pro/links/{proLinkId}/accept:
    post:
      tags: [rf]
      operationId: rfAcceptProLink
      summary: Accept PRO link by partner owner
      security:
        - GatewayAuth: []
      parameters:
        - $ref: "#/components/parameters/RfXGatewayAuth"
        - $ref: "#/components/parameters/RfProLinkId"
      responses:
        "200":
          description: Accepted or already active
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfAcceptProLinkResponse"
        "401":
          $ref: "#/components/responses/RfUnauthorized"
        "403":
          $ref: "#/components/responses/RfForbidden"
        "404":
          $ref: "#/components/responses/RfNotFound"
        "409":
          $ref: "#/components/responses/RfConflict"

components:
  securitySchemes:
    GatewayAuth:
      type: apiKey
      in: header
      name: X-Gateway-Auth

  parameters:
    RfXGatewayAuth:
      name: X-Gateway-Auth
      in: header
      required: true
      schema:
        type: string
    RfIdempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      description: >
        Required idempotency key for claim operation. Same user + same key replays the same
        claim result without creating duplicate business effects.
      schema:
        type: string
        minLength: 1
        maxLength: 160
    RfPartnerId:
      name: partnerId
      in: path
      required: true
      schema:
        type: string
    RfOfferId:
      name: offerId
      in: path
      required: true
      schema:
        type: string
    RfRieltListingId:
      name: listingId
      in: path
      required: true
      schema:
        type: string
    RfVoucherId:
      name: voucherId
      in: path
      required: true
      schema:
        type: string
    RfProLinkId:
      name: proLinkId
      in: path
      required: true
      schema:
        type: string

  responses:
    RfUnauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/RfErrorResponse"
    RfForbidden:
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/RfErrorResponse"
    RfNotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/RfErrorResponse"
    RfValidationError:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/RfErrorResponse"
    RfConflict:
      description: Conflict
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/RfErrorResponse"
    RfRateLimited:
      description: Too many requests
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/RfErrorResponse"
    RfInternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/RfErrorResponse"
    RfServiceUnavailable:
      description: Service unavailable or not configured
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/RfErrorResponse"

  schemas:
    RfHealthResponse:
      type: object
      required: [service, env, status, version]
      properties:
        service:
          type: string
        env:
          type: string
        status:
          type: string
        version:
          type: string

    RfReadyResponse:
      type: object
      required: [service, env, status, version, checks, missing]
      properties:
        service:
          type: string
        env:
          type: string
        status:
          type: string
          enum: [ready, not_ready]
        version:
          type: string
        checks:
          type: object
          additionalProperties:
            type: string
        missing:
          type: array
          items:
            type: string

    RfErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
            message:
              type: string
        requestId:
          type: string
          nullable: true

    RfPartner:
      type: object
      required:
        [id, slug, displayName, countryId, cityId, status, ownerUserId, createdAt, updatedAt]
      properties:
        id:
          type: string
        slug:
          type: string
        displayName:
          type: string
        countryId:
          type: string
        cityId:
          type: string
        atlasPlaceId:
          type: string
          nullable: true
        hostAtlasPlaceId:
          type: string
          nullable: true
        status:
          type: string
          enum: [active, archived]
        ownerUserId:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    RfOffer:
      type: object
      required:
        [id, partnerId, title, offerType, visibility, status, createdByUserId, createdAt, updatedAt]
      properties:
        id:
          type: string
        partnerId:
          type: string
        title:
          type: string
        offerType:
          type: string
          enum: [discount, bundle, gift, access, campaign, event_related]
        visibility:
          type: string
          enum: [public, pro_only, invite_only]
        status:
          type: string
          enum: [draft, active, archived]
        createdByUserId:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    RfRieltListingSummary:
      type: object
      required: [id, title, rfPartnerId]
      properties:
        id:
          type: string
        title:
          type: string
        rfPartnerId:
          type: string
          nullable: true

    RfRieltListingOffer:
      allOf:
        - $ref: "#/components/schemas/RfOffer"
        - type: object
          required: [type, benefit, description, availability, applicabilityNote, priority]
          properties:
            type:
              type: string
              enum: [basic, premium]
            benefit:
              type: string
            description:
              type: string
              nullable: true
            availability:
              type: string
              enum: [available]
            applicabilityNote:
              type: string
              nullable: true
            priority:
              type: integer
              minimum: 0

    RfRieltListingOfferContext:
      type: object
      required: [listing, partner, offers]
      properties:
        listing:
          $ref: "#/components/schemas/RfRieltListingSummary"
        partner:
          anyOf:
            - $ref: "#/components/schemas/RfPartner"
            - type: "null"
        offers:
          type: array
          items:
            $ref: "#/components/schemas/RfRieltListingOffer"

    RfVoucher:
      type: object
      required:
        [
          id,
          offerId,
          partnerId,
          issuedToUserId,
          status,
          claimScope,
          listingContext,
          code,
          claimedAt,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: string
        offerId:
          type: string
        partnerId:
          type: string
        issuedToUserId:
          type: string
        status:
          type: string
          enum: [claimed, redeemed, cancelled]
          description: >
            Runtime status truth for RF voucher lifecycle. `claimed` is the runtime value and maps
            to product semantic "issued" in UX/docs; no separate runtime `issued` enum exists.
        claimScope:
          type: string
          enum: [partner, listing]
          description: >
            Scope used for voucher uniqueness. Partner scope is unique by offer and user;
            listing scope is unique by Rielt listing, offer and user.
        listingContext:
          nullable: true
          anyOf:
            - type: object
              required: [source, listingId, listingTitle]
              properties:
                source:
                  type: string
                  enum: [rielt]
                listingId:
                  type: string
                listingTitle:
                  type: string
                  nullable: true
            - type: "null"
        code:
          type: string
        claimedAt:
          type: string
          format: date-time
        redeemedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        offer:
          type: object
          description: Optional wallet read enrichment for human-readable voucher display.
          required: [id, title, benefit, terms, type]
          properties:
            id:
              type: string
            title:
              type: string
            benefit:
              type: string
            terms:
              type: string
            type:
              type: string
        partner:
          type: object
          description: Optional wallet read enrichment for human-readable partner display.
          required: [id, displayName]
          properties:
            id:
              type: string
            displayName:
              type: string
            cityId:
              type: string
              nullable: true
            countryId:
              type: string
              nullable: true
        validityLabel:
          type: string
          description: Optional human-readable validity note for wallet display.
        usage:
          type: object
          description: Optional human-readable usage guidance for wallet display.
          required: [instruction, contactHint, redeemStatus]
          properties:
            instruction:
              type: string
            contactHint:
              type: string
            redeemStatus:
              type: string

    RfProLink:
      type: object
      required: [id, partnerId, proUserId, status, roleScope, createdAt, updatedAt]
      properties:
        id:
          type: string
        partnerId:
          type: string
        proUserId:
          type: string
        status:
          type: string
          enum: [pending, active, ended]
        roleScope:
          type: string
          enum: [onboarding, curation, promotion, moderation_support, account_support]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    RfPartnerListResponse:
      type: object
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/RfPartner"
        nextCursor:
          type: string
          nullable: true

    RfOfferListResponse:
      type: object
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/RfOffer"
        nextCursor:
          type: string
          nullable: true

    RfVoucherListResponse:
      type: object
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/RfVoucher"
        nextCursor:
          type: string
          nullable: true

    RfVoucherSummary:
      type: object
      required: [totalVouchers, activeVouchers, usedVouchers, cancelledVouchers]
      properties:
        totalVouchers:
          type: integer
          minimum: 0
        activeVouchers:
          type: integer
          minimum: 0
          description: Count of vouchers with runtime status `claimed`.
        usedVouchers:
          type: integer
          minimum: 0
          description: Count of vouchers with runtime status `redeemed`.
        cancelledVouchers:
          type: integer
          minimum: 0
          description: Count of vouchers with runtime status `cancelled`.

    RfProLinkListResponse:
      type: object
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/RfProLink"
        nextCursor:
          type: string
          nullable: true

    RfClaimResponse:
      type: object
      required: [voucher, idempotentReplay]
      properties:
        voucher:
          $ref: "#/components/schemas/RfVoucher"
        idempotentReplay:
          type: boolean

    RfRedeemResponse:
      type: object
      required: [voucher, applied]
      properties:
        voucher:
          $ref: "#/components/schemas/RfVoucher"
        applied:
          type: boolean

    RfAcceptProLinkResponse:
      type: object
      required: [proLink, applied]
      properties:
        proLink:
          $ref: "#/components/schemas/RfProLink"
        applied:
          type: boolean

    RfCreatePartnerRequest:
      type: object
      required: [displayName, countryId, cityId]
      properties:
        displayName:
          type: string
        countryId:
          type: string
        cityId:
          type: string
        atlasPlaceId:
          type: string
          nullable: true
        hostAtlasPlaceId:
          type: string
          nullable: true

    RfCreateOfferRequest:
      type: object
      required: [title, offerType, visibility]
      properties:
        title:
          type: string
        offerType:
          type: string
          enum: [discount, bundle, gift, access, campaign, event_related]
        visibility:
          type: string
          enum: [public, pro_only, invite_only]

    RfCreateProLinkRequest:
      type: object
      required: [partnerId, roleScope]
      properties:
        partnerId:
          type: string
        roleScope:
          type: string
          enum: [onboarding, curation, promotion, moderation_support, account_support]
