# Guru Asia — UI Structure

> Основано на существующем прототипе Guru (карта слева + список справа) и общих UX‑паттернах Go2Asia.

## 1. Layout модуля

- **Шапка PWA** (общая для всех модулей)
  - Логотип Go2Asia.
  - Главная навигация: *Atlas, Pulse, Blog, Space*.
  - Иконка глобуса/смены страны.
  - Иконка поиска (глобальный поиск по платформе).
  - Иконка профиля / входа.

- **Hero‑полоса модуля Guru Asia**
  - Название: **Guru Asia**.
  - Подзаголовок: «AI‑путеводитель и навигатор рядом с вами».
  - Краткое описание: 1–2 строки (адаптировано из go2asia_overview).
  - Бейдж статуса местоположения: «📍 Местоположение определено / требуется доступ».

- **Основной двухколоночный layout**
  - **Левая колонка** — карта (OpenStreetMap/Leaflet).
  - **Правая колонка** — лента объектов (Guru Feed).

## 2. Верхняя панель фильтров и настроек

Располагается над картой и лентой (sticky).

Элементы:

1. **Селектор радиуса**
   - Chips: `500 м`, `1 км`, `2 км`, `5 км`.
   - + иконка «параметры» для ввода кастомного радиуса.

2. **Селектор времени**
   - Toggle: `Сейчас`, `Сегодня`, `Вечером`, `Выбрать диапазон`.
   - При выборе диапазона открывается date/time‑picker.

3. **Фильтр типов**
   - Мультичипы: `Места`, `События`, `Партнёры RF`, `Квесты`, `Жильё`.
   - Подсветка активных.

4. **Фильтр категорий**
   - Горизонтальный список скроллируемых чипов: `Еда`, `Бар`, `Кофе`, `Экскурсии`, `Работа`, `Для детей`, `Ночная жизнь`, `Спорт` и т.п.

5. **Фильтры качества**
   - Тогглы: `Проверено редакцией`, `Только Russian Friendly`, `С ваучерами`.

6. **Селектор сортировки**
   - Dropdown: `По расстоянию`, `По времени`, `По рейтингу`, `По цене`.

7. **Кнопка AI‑подсказки**
   - Primary‑кнопка/чип: «✨ Подобрать мне план».
   - При нажатии отправляется запрос к `/api/guru/ai/suggestions` и в правой колонке показывается блок с рекомендованным сценарием (над лентой).

## 3. Карта (левая колонка)

- Базовый слой: OpenStreetMap.
- Зум и перетаскивание.
- Синий круг радиуса вокруг текущего положения.
- Маркеры объектов:
  - Цвет/иконка зависят от типа (`место`, `событие`, `партнёр`, `квест`, `жильё`).
  - Hover/клик подсвечивают соответствующую карточку справа.
- Tooltip/попап:
  - Название объекта.
  - Краткая информация (тип, рейтинг, иконка ваучера).
  - CTA: «Подробнее» → скролл к карточке / переход в модуль Atlas/Pulse.

## 4. Лента объектов (правая колонка)

### 4.1. Структура ленты

1. **Блок AI‑подсказки (если активен)**
   - Карточка сценария с заголовком, описанием и списком 3–6 объектов (мини‑карточки).
   - Кнопка: «Применить план» — проскроллить к этим объектам и подсветить их на карте.

2. **Секция “Сейчас рядом с вами”**
   - Основные объекты по умолчанию сортируются по расстоянию.
   - Каждая карточка:
     - Заголовок + иконка типа.
     - Подзаголовок (короткое описание).
     - Бейджи: `Партнёр RF`, `Проверено редакцией`, `Квест`, `Событие сегодня`.
     - Информация:
       - расстояние/время пешком,
       - рейтинг и число отзывов,
       - диапазон цен или пометка `Бесплатно`,
       - актуальный статус (`Открыто до 22:00`, `Начало в 19:00`).
     - Действия:
       - «Открыть» — переход в соответствующий модуль.
       - «Сохранить» (иконка закладки) → Reactions Service.
       - «Поделиться» (иконка) → репост в Space (через Reactions/Space).

3. **Секция “События сегодня/скоро”**
   - Выделенный блок для событий из Pulse Asia в выбранном радиусе и временном окне.

4. **Секция “Russian Friendly рядом”**
   - Подборка мест‑партнёров, особенно если включен фильтр RF.

5. **Секция “Жильё и коворкинги”**
   - Подборка мест для работы/жилья (интеграция с Rielt/Marketplace).

6. **Пагинация / бесконечный скролл**
   - При пролистывании вниз автоматически загружаются следующие объекты (`limit/offset`).

### 4.2. Карточка объекта (пример поля)

- Заголовок: `Anna — гид по Новосибирску` / `Coffee Bean`.
- Подзаголовок: «Экскурсия по центру», «Кофейня рядом с вами».
- Бейджи ролей: `PRO`, `VIP` (если объект — PRO‑гид).
- Дистанция: `276 м — 3 мин пешком`.
- Цена: `от 1500 ₽` / `Бесплатно`.
- Краткое описание: 1–2 строки.
- Кнопка `Открыть` (primary) + вторичные иконки.

## 5. Пустые состояния и ошибки

- **Нет доступа к геолокации**
  - Большой блок с иллюстрацией и текстом:
    - «Чтобы Guru подсказал, что рядом, разрешите доступ к геолокации».
    - Кнопка: «Разрешить доступ».
    - Альтернатива: «Выбрать город вручную» → ссылка в Atlas.

- **Ничего не найдено**
  - Текст: «В выбранном радиусе и фильтрах ничего нет».
  - Предложения:
    - увеличить радиус (кнопки `+500 м`, `+1 км`),
    - сбросить часть фильтров,
    - перейти в Atlas/Pulse для общего поиска.

- **Ошибка API**
  - Сообщение о технической проблеме, кнопка «Повторить запрос».

## 6. Мобильная версия

- Layout переключается на **вертикальный стек**:
  - Сверху — компактная hero‑полоса + фильтры в один/два ряда.
  - Далее — переключатель `Карта` / `Список` (табы).
  - В режиме карты список сворачивается в нижний sheet (bottom sheet), который можно тянуть вверх.
- Кнопка AI‑подсказки фиксируется внизу экрана.

## 7. Встраивание в PWA‑оболочку

- Route: `/guru`.
- Guru использует общие компоненты:
  - `ModuleHero` для шапки,
  - `FilterChips`, `Card`, `BottomSheet`, `MapContainer` из `@go2asia/ui`,
  - общие токены отступов, радиусов и размеров шрифта.
- Состояние и контекст Guru должны быть изолированы от других модулей, но совместимы с глобальным поиском и Reactions/Connect для наград за действия.

