# Identity/RF Seed Baseline — Intentionally Deferred (v1)

Этот список намеренно не входит в текущий seed baseline:

- Полный social graph (friends/follows/recommendation graph).
- Полное заполнение Space profile universe и rich profile onboarding.
- Advanced moderation/admin tooling для всех сервисов.
- Full RF v2 dataset (расширенные каталоги, полная операционная модель).
- Массовый voucher lifecycle import (большие исторические датасеты).
- Полный review universe и content moderation seed.
- Большой multi-country import pack по всем городам/странам.
- Любые speculative future fields, не используемые в текущем runtime first slice.
- Отдельный автоматизированный массовый seed/import pipeline.

