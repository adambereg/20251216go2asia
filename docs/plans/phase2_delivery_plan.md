# Фаза 2 Go2Asia — обновлённый план реализации (Delivery Plan)

Дата: 2025-12-26  
Роль: Lead Architect + Delivery Planner  
SSOT факта по сервисам: `docs/ops/service_inventory.md`  
Ключевые ADR:  
- `docs/decisions/adr_0020_no_inline_comments_social_first.md` (no inline comments)  
- `docs/decisions/adr_0021_token_points_connect_terminology.md` (Token/Points/Connect)  
- `docs/decisions/adr_0022_space_services_naming_and_boundaries_mvp.md` (Space/Content/Feed/Reactions naming)  

---

## 1) Рамки Фазы 2 (обновлённые)

### 1.1 Interface-aware delivery (важно)

В Go2Asia “интерфейсы” — это **классы UI‑поверхностей доступа и управления**, которые живут **над** модулями и сервисами.  
Интерфейсы **≠ модули ≠ сервисы** (см. `docs/knowledge/interface_concept.md`).

Классы интерфейсов экосистемы:
- **User Cabinet**: личный кабинет пользователя (Spacer/VIP/PRO как пользователь).
- **PRO Console**: рабочая консоль PRO‑спейсеров (кураторство/операционные действия).
- **Business Console**: кабинет бизнес‑партнёра (управление профилем/оферами RF).
- **Admin Console**: модерация/редактура/support/ops (внутренние инструменты).

Принцип доступа (важно для Фазы 2):
- **PRO Console и Business Console — полноценные интерфейсы**, но это **не отдельные “аккаунты”**.
- Повышение до PRO / назначение бизнес‑доступа — это **отзывные права/энтитлменты**: доступ к соответствующей консоли может быть **отозван** без удаления пользовательского аккаунта и без потери данных User Cabinet.

Разграничение настроек (важно для Space и User Cabinet):
- **Account settings**: глобальные настройки аккаунта (профиль, безопасность, язык, общая приватность) — управляются в **User Cabinet**.
- **Module settings**: настройки конкретного модуля (например, Space settings: лента/уведомления Space/видимость в Space) — управляются **внутри UI модуля**.

### 🎯 Что считается успешным завершением Фазы 2

Фаза 2 считается завершённой, когда:

- **Space (Social Core)** работает на реальном backend’е: посты/репосты, лента, реакции/ответы (social-first), базовые группы/профиль, без inline-комментариев “под объектами” контентных модулей.
- **Quest (Gamification Core)** работает на реальном backend’е: квесты, прогресс, завершение квеста как событие награждения **Points**.
- **Rielt (Practical Domain)** работает на реальном backend’е: чтение/поиск объявлений, базовый CRUD (минимум — для PRO/owner), связка с гео-контентом (через `content-service`).
- **Guru (Aggregation UX)** работает как агрегатор (BFF): отдаёт смешанную выдачу “рядом” из реально работающих доменов.
- **RF (Economic Hub)** введён как **capstone** в **PARTIAL** режиме: бизнес/PRO/VIP сценарии работают, но без полной токеномики и без on-chain.

### 🟡 Что осознанно остаётся неполным до Фазы 3

- **Полная токеномика (G2A / NFT / on-chain)**: не реализуется в Фазе 2 (только **Points**).
- **Полный “Tokenomics engine”** (Token/Connect Service как расчётный центр): **LATER / PHASE 3** (в repo `apps/token-service` — skeleton).
- **Notification Service / Event Bus / Moderation & Trust&Safety как сервисы**: **LATER / PHASE 3** (в Фазе 2 допускается минимум синхронных интеграций).
- **Advanced monetization (VIP/PRO billing), аналитика, биллинг**: **PHASE 3+**.

### ✅ Почему это допустимо

- В Фазе 2 мы закрываем **ядро продуктовых контуров** (social + gamification + practical domains) и вводим RF как multi‑sided модуль, но делаем его “экономику” на **Points**.
- Всё, что требует on-chain/юридики/сложных правил распределения (G2A/NFT), переносится в Фазу 3 без попытки “симулировать” это в Фазе 2.

---

## 2) Platform Layer (обязательный)

> Эти milestone не являются “модулями”.  
> Это платформа‑подготовка, которая **делается до** Space/Quest/Rielt/Guru/RF, чтобы продуктовые вертикали собирались одинаково и быстро.

### Milestone 2.0 — Foundations / Service Readiness

**Цель:** подготовить “конвейер” для новых сервисов Фазы 2 (Worker‑сервисы + БД + OpenAPI + SDK + gateway routing) на едином шаблоне.

**Что делаем (platform enablers):**
- **Service template (apps/*)**: единый каркас Worker‑сервисов (health/version, requestId/logging, gateway-origin trust model, service-to-service auth где нужно).
- **Gateway routing**: заранее определить и закрепить новые префиксы в `api-gateway` (например, `/v1/space/*`, `/v1/quest/*`, `/v1/rielt/*`, `/v1/guru/*`, `/v1/rf/*`) и правила auth‑прокидывания `X-User-ID`.
- **DB readiness**: единый способ добавления схем/миграций в `packages/db` (Drizzle), соглашение “schema-by-domain”.
- **OpenAPI-first дисциплина**: новые эндпоинты добавляются в bundle (`docs/openapi/openapi.bundle.yaml`) параллельно реализации.
- **SDK readiness**: расширение `@go2asia/sdk` по мере появления новых API (через mutator + auth).
- **Naming/Boundaries**: закрепить, что `apps/content-service` = Atlas/Pulse/Blog домен, а Space backend = `space-service` (ADR‑0022), и что Connect/Points/Token терминология не конфликтует (ADR‑0021).

**Факт repo (что уже есть и “хардним”):**
- `apps/api-gateway`, `apps/auth-service`, `apps/content-service`, `apps/points-service`, `apps/referral-service` (см. `docs/ops/service_inventory.md`).

**PHASE 3 (не делаем в 2.0):**
- Event Bus, Notification Service, Tokenomics engine, on-chain.

---

### Milestone 2.1 — Connect (hardening поверх Points/Referral)

**Цель:** сделать Connect “платформенным” интерфейсом экономики и прогресса на базе **реально существующих** `points-service` и `referral-service` (без притворства про Tokenomics engine).

**Что делаем (platform enablers):**
- **Стабилизировать контракт Points/Referral**:
  - гарантировать idempotency (у Points уже SSOT по `externalId`),
  - договориться о минимальном наборе “actions”/событий, которые будут использоваться модулями Фазы 2 (Space/Quest/RF/Rielt).
- **Connect UX hardening**:
  - Connect показывает баланс/транзакции/рефералы строго через `/v1/points/*` и `/v1/referral/*`,
  - фиксируем терминологию “Points Service” (ADR‑0021).
- **Подготовить расширение Points без токеномики**:
  - допускается расширение списка `action` в `points-service` под события Space/Quest/RF,
  - но **без** G2A/NFT и без конверсий.

**Существующие сервисы, на которых стоит milestone:**
- `apps/points-service`, `apps/referral-service`, `apps/auth-service`, `apps/api-gateway`.

**PHASE 3 (не делаем в 2.1):**
- Tokenomics engine (“Token Service” как расчётный центр), биллинг VIP/PRO, on-chain.

---

### Milestone 2.2 — Media / Storage (R2, signed uploads, SDK)

**Цель:** дать всем продуктовым модулям единый способ работать с медиа (фото/видео) без “уникальных решений” в каждой вертикали.

**Что делаем (platform enablers):**
- **R2 readiness**:
  - единый паттерн “байты в R2, метаданные в Neon”,
  - контракты signed upload (или upload proxy) для клиента.
- **Media contracts**:
  - минимум: загрузка/публикация медиа для Space и RF (картинки постов, офферов, ваучеров),
  - затем — расширение на Rielt (фото листинга).
- **SDK support**:
  - добавить client helpers для загрузок (pre-signed flow) в `@go2asia/sdk`.

**Существующие сервисы, которые используются:**
- `apps/content-service` уже содержит `media_files` как метаданные (см. `packages/db/src/schema/content.ts`) — использовать как основу.

**PHASE 3 (не делаем в 2.2):**
- сложные CDN‑пайплайны, DRM, продвинутый антифрод загрузок.

---

## 3) Product Layer (вертикальные модули)

### 3.0 Покрытие интерфейсов по модулям (Фаза 2)

> Таблица показывает **какие интерфейсы используют модуль** в рамках Фазы 2.  
> Это не означает “отдельные сервисы под интерфейсы”: в Фазе 2 интерфейсы — это слои UI над теми же доменными API.

| Модуль | User Cabinet | PRO Console | Business Console | Admin Console | Примечания (scope) |
|---|---|---|---|---|---|
| Space | Да | Частично | Нет | Частично | Соц‑контур для всех: пользовательский UX обязателен; модерация/ops — минимально (LATER расширение) |
| Quest | Да | Да | Нет | Частично | PRO управляет квестами; Admin — минимальный ops (если нужен) |
| Rielt | Да | Частично | Нет | Частично | Пользовательский просмотр обязателен; PRO/ops — минимальный CRUD/модерация |
| Guru | Да | Нет | Нет | Нет | В Фазе 2 — “витрина/входная точка” для пользователей, без админ‑инструментов |
| RF (PARTIAL) | Да | Да | Да | Частично | Capstone Фазы 2: Business/PRO/VIP сценарии работают, но **Points-only** |

### 3.1 Space (social core)

- **Цель в Фазе 2**: единый social‑контур платформы (обсуждения через репосты/реакции; no inline comments).
- **Интерфейсы (Фаза 2)**:
  - **User Cabinet**: вход в социальный контур (профиль пользователя, личная активность/ссылки), без подмены UI самого Space.
  - **PRO Console**: частично (минимальные операционные действия в Space, если требуется для кураторства/контента).
  - **Admin Console**: частично (минимальные ops/модерационные действия; полноценная админка — LATER).
- **Реализуется в Фазе 2**
  - посты/репосты как объекты Space,
  - лента (MVP),
  - реакции/ответы к постам/репостам,
  - базовые профили/группы (минимум),
  - Points за ограниченный набор social‑событий (через `points-service`).
- **LATER / PHASE 3**
  - Notification Service, Event Bus, Realtime chat, полный anti-fraud.

### Milestone 2.3 — Space Service (social core backend)

- **Новые backend-сервисы (apps/*)**
  - `apps/space-service` (**новый**) — Space Service (UGC posts/reposts + feed + reactions как модули, см. ADR‑0022).
- **Используем существующие сервисы**
  - `apps/api-gateway` — проксирование `/v1/space/*` → `SPACE_SERVICE_URL`
  - `apps/auth-service` — user ensure/роль (MVP)
  - `apps/points-service` — начисление Points (через internal endpoint) **только off-chain**
- **Нереализованы, но ожидаемы (PHASE 3)**
  - `notification-service`, `event-bus`, `moderation/trust-safety`
- **Зависимости от токеномики**
  - **PHASE 3**: любые G2A/NFT rewards запрещены; в 2.3 только Points.
- **Definition of Done (DoD)**
  - **Модуль**: Space поддерживает social-first (репосты/реакции/ответы) без inline-комментариев под объектами других модулей.
  - **Интерфейсы**: есть рабочие пользовательские сценарии Space (как минимум через User Cabinet / пользовательские поверхности), а также зафиксирован минимальный операционный доступ для PRO/Admin (без расширения в отдельные сервисы).
  - **Настройки**: соблюдено разграничение “Account settings (User Cabinet)” vs “Space settings (внутри Space UI)”.

---

### 3.2 Quest (gamification)

- **Цель в Фазе 2**: квесты как генератор активности и событий для Points.
- **Интерфейсы (Фаза 2)**:
  - **User Cabinet**: пользователь видит свой прогресс/историю/награды и точки входа в прохождение.
  - **PRO Console**: создание/ведение квестов (основной рабочий интерфейс квестов в Ф2).
  - **Admin Console**: частично (ops‑вмешательства при необходимости; полноценная админка — LATER).
- **Реализуется в Фазе 2**
  - Quest/Steps/Progress,
  - quest_completed → Points,
  - отчёт в Space.
- **PHASE 3**
  - NFT/on-chain награды, сложные распределения.

### Milestone 2.4 — Quest Service (gamification core backend)

- **Новые backend-сервисы**
  - `apps/quest-service` (**новый**) — квесты/прогресс.
- **Используем существующие**
  - `points-service` (Points за `quest_completed`)
  - `space-service` (пост/репост отчёта)
  - `content-service` (валидация шагов по местам/событиям — read-only в MVP)
- **Ожидаемы в PHASE 3**
  - NFT Service / Tokenomics engine (награды уровня “NFT/G2A”)
- **Definition of Done (DoD)**
  - **User Cabinet**: пользователь видит прогресс и результат (включая начисленные Points через Connect).
  - **PRO Console**: PRO может управлять квестами в пределах возможностей Фазы 2.
  - **Ограничение Ф2**: награды только Points (без tokenomics engine / G2A / NFT).

---

### 3.3 Rielt (practical domain)

- **Цель в Фазе 2**: практический домен “жильё”, который даёт ценность и питается Guru.
- **Интерфейсы (Фаза 2)**:
  - **User Cabinet**: пользовательский просмотр/поиск и “мои действия” (без смешения с PRO‑инструментами).
  - **PRO Console**: частично (минимальные сценарии управления объявлениями для PRO/owner).
  - **Admin Console**: частично (минимальные модерационные/ops сценарии; LATER расширение).
- **Реализуется в Фазе 2**
  - read/search listings,
  - минимальный create/update (PRO/owner),
  - привязка к географии через `content-service`.
- **PHASE 3**
  - сделки/платежи/биллинг, расширенная верификация.

### Milestone 2.5 — Rielt Service (practical domain backend)

- **Новые backend-сервисы**
  - `apps/rielt-service` (**новый**) — listings.
- **Используем существующие**
  - `content-service` (гео-справочник через `/v1/content/*`)
  - `auth-service` (роль/права)
- **Ожидаемы в PHASE 3**
  - billing/monetization, расширенная верификация, коммуникационные сценарии.
- **Definition of Done (DoD)**
  - **User Cabinet**: пользователь может просматривать/искать объявления и видеть историю своих действий (если предусмотрено UI‑слоем).
  - **PRO Console**: есть минимальные сценарии управления объявлениями в рамках ролей PRO/owner.
  - **Ограничение Ф2**: без сделок/платежей/биллинга.

---

### 3.4 Guru (aggregator UX)

- **Цель в Фазе 2**: единая точка входа “что вокруг меня”, агрегирующая домены.
- **Интерфейсы (Фаза 2)**:
  - **User Cabinet**: основная пользовательская витрина/точка входа.
- **Реализуется в Фазе 2**
  - Guru Service как BFF `/nearby`,
  - graceful degradation.
- **PHASE 3**
  - персонализация/AI, notification pipeline.

### Milestone 2.6 — Guru Service (aggregation backend)

- **Новые backend-сервисы**
  - `apps/guru-service` (**новый**) — агрегатор `/nearby`.
- **Используем существующие**
  - `content-service` (places/events/articles)
  - `rielt-service` (listings)
  - `quest-service` (чекпоинты/квесты)
  - `rf-service` (после 2.7)
- **Ожидаемы в PHASE 3**
  - персонализация, notification pipeline, search service (если вводится отдельно).
- **Definition of Done (DoD)**
  - **User Cabinet**: выдача “рядом” собирается из реально работающих доменов и ведёт в соответствующие модули.
  - **Ограничение Ф2**: без персонализации/ML и без отдельного notification‑контура.

---

### 3.5 RF (capstone, **PARTIAL**)

- **Цель в Фазе 2**: ввести RF как multi‑sided hub (Business↔PRO↔VIP), но **PARTIAL**, без on-chain и без полной экономики.
- **Интерфейсы (Фаза 2)**:
  - **User Cabinet**: VIP/Spacer сценарии (каталог/claim/redeem/история/ссылки), без превращения RF в “каталог-only”.
  - **PRO Console**: онбординг и кураторство партнёров (операционный рабочий интерфейс).
  - **Business Console**: управление профилем/ваучерами и подтверждение погашений (операционный рабочий интерфейс).
  - **Admin Console**: частично (минимальные ops/поддержка; LATER расширение).
- **Реализуется в Фазе 2 (PARTIAL / capped)**
  - каталог партнёров/ваучеров,
  - claim/redeem ваучеров (Points или бесплатно),
  - Business/PRO workflows (MVP),
  - social‑сигналы через Space (репосты/реакции),
  - награждение только Points.
- **PHASE 3**
  - G2A/NFT, комиссии/выплаты, сложные KPI‑расчёты, биллинг VIP/PRO.

### Milestone 2.7 — RF Service (**PARTIAL capstone**) + интеграции

- **Новые backend-сервисы**
  - `apps/rf-service` (**новый**) — партнёры + ваучеры + PRO/Business workflows (в MVP допускается объединение Partner/Voucher в одном сервисе).
- **Используем существующие**
  - `space-service` (social‑обсуждения и UGC‑сигналы через repost/reactions)
  - `quest-service` (квесты вокруг партнёров/ваучеров)
  - `points-service` (Points за события RF: verification, voucher_claimed/redeemed — **без G2A/NFT**)
  - `content-service` (привязка партнёров к географии)
- **Ожидаемы в PHASE 3**
  - Tokenomics engine (G2A/NFT, сложные KPI/комиссии)
  - Blockchain gateway / NFT
  - полноценная модерация/anti-fraud и уведомления
- **Definition of Done (DoD)**
  - **User Cabinet (VIP/Spacer)**: пользовательские сценарии RF работают в рамках **Points-only**.
  - **PRO Console**: PRO может онбордить/курировать партнёров в рамках возможностей Фазы 2.
  - **Business Console**: бизнес может управлять профилем/оферами и подтверждать погашения в рамках возможностей Фазы 2.
  - **Ограничение Ф2**: RF остаётся **PARTIAL-capstone** (без tokenomics engine / G2A / NFT / биллинга VIP/PRO).

---

## 4) Integration & E2E (сквозные сценарии)

**Цель:** убедиться, что платформа и вертикали собираются в единый продукт без “дыр” в интеграциях.

Минимальные E2E сценарии Фазы 2:
- **Space**: создание поста/репоста → появляется в ленте → реакция/ответ → начисление Points (если включено правилом).
- **Quest**: старт квеста → выполнение шагов → quest_completed → начисление Points → пост/репост отчёта в Space.
- **Rielt**: создание/обновление листинга (PRO) → листинг читается → отображается в Guru (через `/nearby`).
- **RF (PARTIAL)**: онбординг партнёра (PRO) → публикация ваучера (Business/PRO) → claim (VIP) → redeem (Business) → отображение social‑сигналов через Space.

Артефакты согласования:
- `docs/openapi/openapi.bundle.yaml` должен включать новые `/v1/space/*`, `/v1/quest/*`, `/v1/rielt/*`, `/v1/guru/*`, `/v1/rf/*`.
- `docs/ops/service_inventory.md` обновляется при появлении новых `apps/*`.

---

## 5) Выход из Фазы 2 → вход в Фазу 3 (чётко)

### Чем Фаза 2 заканчивается

- Выполнены platform enablers **2.0–2.2**.
- Запущены продуктовые вертикали **2.3–2.7**: Space → Quest → Rielt → Guru → RF (**PARTIAL**).
- Экономика Фазы 2 = **Points** (реальный `points-service`), без on-chain и без “полного Tokenomics engine”.

### С чем проект входит в Фазу 3 (какие “достройки” ожидаемы)

- Реальный Tokenomics engine (не skeleton), NFT, Blockchain Gateway.
- Notification Service + Event Bus + Trust&Safety как инфраструктурные сервисы.
- Монетизация статусов VIP/PRO (если подтверждена продуктом) — как отдельный контур PHASE 3.



