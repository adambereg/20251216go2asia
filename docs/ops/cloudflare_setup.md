# Cloudflare Setup — Настройка Cloudflare для экосистемы Go2Asia

Cloudflare используется как единая точка входа для трафика Go2Asia, обеспечивая:
- безопасное и быстрое распределение запросов,
- защиту от атак и нежелательного трафика,
- SSL/TLS-шифрование,
- управление DNS,
- ускорение загрузки контента,
- улучшенную доступность для пользователей из разных регионов (Россия, Вьетнам, Таиланд и др.).

Документ описывает стандарты и рекомендации для настройки Cloudflare в контексте Go2Asia.

---

# 1. Роль Cloudflare в архитектуре Go2Asia

Cloudflare стоит между пользователями и инфраструктурой:

Пользователь → Cloudflare → Netlify (Frontend)
                     ↓
                  API Gateway (Cloudflare Workers or Backend Origin)
                     ↓
               Backend Microservices

Через Cloudflare проходят:

- основной домен `go2asia.app`,
- production, staging и preview-деплои фронтенда,
- API-трафик (если используется Cloudflare Worker Gateway),
- статические ресурсы,
- аналитика трафика и угроз.

---

# 2. DNS-конфигурация

## 2.1. Основные правила DNS

- Все DNS-записи **должны** быть управляемы только через Cloudflare.
- Включён **orange cloud** (проксирование) для:
  - `go2asia.app`,
  - `www.go2asia.app`,
  - `api.go2asia.app` (если используется),
  - `staging.go2asia.app`.

Это обеспечивает:
- WAF,
- кэширование,
- защиту от DDoS,
- скрытие IP-адресов origin-серверов.

## 2.2. Примеры DNS-записей

| Поддомен | Тип | Значение | Proxy | Назначение |
|---------|-----|----------|--------|------------|
| go2asia.app | CNAME | `project-name.netlify.app` | 🟧 | прод-фронтенд |
| www.go2asia.app | CNAME | `go2asia.app` | 🟧 | редирект на основной домен |
| staging.go2asia.app | CNAME | staging‑…netlify.app | 🟧 | staging окружение |
| api.go2asia.app | CNAME / A | API Gateway | 🟧 | будущий API-шлюз |

---

# 3. SSL/TLS конфигурация

## 3.1. Рекомендуемый режим: **Full (strict)**

Причины:
- максимальная безопасность,
- защита от MITM,
- исключение возможности подачи некорректного сертификата со стороны origin.

## 3.2. Always Use HTTPS

- Всегда включено.
- HTTP-запросы перенаправляются на HTTPS.

## 3.3. HSTS (опционально)

Для production рекомендуется:

- включить HSTS,
- срок — не менее 6 месяцев,
- включить поддомены.

**Важно:**  
включать только после полной стабильности конфигурации!

---

# 4. Кеширование (Caching)

Cloudflare может значительно ускорить загрузку Go2Asia.

## 4.1. Что кешируем?

- статические файлы (JS, CSS, изображения),
- публичный контент (страницы Atlas/Blog/Events для гостей),
- ответы Netlify на статические запросы.

## 4.2. Что НЕ кешируем?

- API запросы, требующие живых данных,
- запросы с авторизованными сессиями,
- персональный контент пользователя.

## 4.3. Рекомендуемые настройки Caching Level:

- **Standard** или **Caching Everything** (для статики),
- для PWA shell использовать **Edge Cache TTL = 1 час**,  
  при этом разделять статику по хэшам (Netlify делает это автоматически).

---

# 5. Page Rules / Rulesets

Используются для:

- кэширования определённых URL,
- редиректов (например, `www → root domain`),
- усиления безопасности API.

## Пример правил:

1. `*go2asia.app/_next/static/*`  
   Cache Level: Cache Everything  
   Edge TTL: 1 месяц  

2. `*go2asia.app/api/*`  
   Cache Level: Bypass  
   Security Level: High  

3. `www.go2asia.app/*`  
   Forwarding URL → https://go2asia.app/$1

---

# 6. WAF (Web Application Firewall)

Cloudflare WAF обеспечивает защиту:

- от SQL Injection,
- от XSS,
- от ботов и сканеров,
- от попыток взлома API.

## 6.1. Рекомендации для Go2Asia

- включить OWASP Core Ruleset,
- включить «Bot Fight Mode» (если нет проблем с SEO),
- включить режим блокировки для критичных endpoint’ов:
  - `/api/auth/*`,
  - `/api/payments/*`,
  - `/api/referral/*`.

## 6.2. Действие при ложных срабатываниях

- Использовать режим «Log» для тестирования перед включением «Block».
- Добавлять точечные исключения по пути/методу/IP.

---

# 7. Rate Limiting (очень важно для API)

Для защиты API Go2Asia от брутфорса и флуд-атак:

| Endpoint | Лимит | Интервал |
|----------|--------|-----------|
| `/api/auth/login` | 5 | 1 минута |
| `/api/auth/send-code` | 3 | 5 минут |
| поиск `/api/places/search` | 20 | 1 минута |
| `/api/events` | 60 | 1 минута |

---

# 8. Cloudflare Zero Trust (опционально)

Для внутренних инструментов (публично недоступных):

- админ-панели,
- dev-инстансы,
- dashboard для микросервисов.

Cloudflare Access позволяет:

- ограничивать доступ по OAuth (Google/VK),
- проверять IP,
- использовать одноразовые токены.

---

# 9. Особенности работы с РФ-аудиторией

Некоторые российские провайдеры могут периодически:

- блокировать IP Cloudflare,
- снижать скорость CDN,
- давать нестабильную маршрутизацию.

## Рекомендации:

### 9.1. Постоянный мониторинг доступности РФ-трафика  
Использовать внешние мониторинги:
- Selectel,
- G-Core,
- VPS в разных регионах РФ.

### 9.2. Возможность fallback-домена  
На будущее возможно:
- выделить домен без Cloudflare-проксирования,
- использовать альтернативный маршрут.

### 9.3. Усиленное кэширование публичных страниц  
Особенно полезно для:
- Atlas,
- Pulse,
- Blog,
- RF-каталога.

---

# 10. Логи и наблюдаемость

Cloudflare предоставляет:

- Analytics,
- Security Insights,
- Caching Reports.

Рекомендуется:

- еженедельная проверка отчётов,
- анализ DDoS/бот-активности,
- включение email-alerts при аномалиях.

---

# 11. Чек-лист Cloudflare Setup

### DNS  
- [ ] Все записи управляются через Cloudflare  
- [ ] Проксирование включено (🟧)  
- [ ] Staging и API поддомены корректны  

### SSL/TLS  
- [ ] Full (strict)  
- [ ] Always Use HTTPS  
- [ ] HSTS включён (если стабильно)  

### Кеширование  
- [ ] Статика кешируется  
- [ ] API не кешируется  
- [ ] Настроены правила для `_next/static/*`  

### WAF  
- [ ] OWASP включён  
- [ ] Bot Fight Mode включен  
- [ ] Правила для auth/payment endpoint'ов  

### Rate Limiting  
- [ ] Лимиты на login, search, events  

### РФ-специфика  
- [ ] Мониторинг доступности  
- [ ] Fallback-план (на будущее)  

---

# 12. Связанные документы

- `netlify_setup.md`
- `ci_cd.md`
- `environments.md`
- `monitoring.md`
- `logging.md`
- `../../architecture/api_architecture.md`

---

## 13. Переиспользование существующей инфраструктуры Cloudflare

⚠️ Важно: инфраструктура Cloudflare для Go2Asia **уже развёрнута** и используется как источник правды.  
Новый код и новые пайплайны должны **подключаться к существующим ресурсам**, а не создавать новые.

### 13.1. Уже созданные сущности

- **Аккаунт Cloudflare**  
  - Account ID: см. Cloudflare Dashboard (фиксируется в GitHub Secrets переменной `CLOUDFLARE_ACCOUNT_ID`)

- **Workers (staging)**  
  - `go2asia-api-gateway-staging`
  - `go2asia-auth-service-staging`
  - `go2asia-content-service-staging`
  - `go2asia-referral-service-staging`
  - `go2asia-token-service-staging`

- **DNS-зона: `go2asia.space`**  
  Уже настроены CNAME-записи (примерный набор):
  - `api.go2asia.space` → `api-gateway.go2asia.workers.dev`
  - `auth.go2asia.space` → `auth-service.go2asia.workers.dev`
  - `content.go2asia.space` → `content-service.go2asia.workers.dev`
  - `token.go2asia.space` → `token-service.go2asia.workers.dev`
  - `rf-api.go2asia.space` → `rf-api.go2asia.workers.dev`
  - и др. служебные поддомены (см. реальную зону DNS в Cloudflare).

- **R2 Object Storage**
  - Bucket: `go2asia-media`
  - Типовые префиксы: `country/`, `city/`, `place/`
  - Используется для хранения медиа-контента (страны, города, места и т.п.).

### 13.2. Правила для разработки и CI/CD

1. **Не создавать новые Workers с другими именами**, если это не зафиксировано в ADR.
2. При изменении кода:
   - деплой происходит в уже существующие приложения (`go2asia-*-service-staging`),
   - GitHub Actions должны ссылаться на эти имена, а не генерировать новые.
3. При работе с R2:
   - использовать существующий bucket `go2asia-media`,
   - не создавать дополнительные бакеты без отдельного архитектурного решения.
4. Любые изменения инфраструктуры Cloudflare (новые Workers, DNS, R2) должны:
   - обсуждаться через ADR (`docs/decisions/*.md`),
   - фиксироваться в этом документе (`cloudflare_setup.md`) и `docs/ops/environments.md`.

Таким образом, новый код «приезжает» в уже готовую Cloudflare-инфраструктуру и не ломает текущие настройки DNS, Workers и R2.
