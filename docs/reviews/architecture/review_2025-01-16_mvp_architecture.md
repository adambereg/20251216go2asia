# Ревью архитектуры MVP Go2Asia

**Дата:** 2025-01-16  
**Ревьюер:** Architecture Reviewer  
**Документы:**
- `docs/architecture/mvp_architecture.md`
- `docs/architecture/mvp_data_schemas.md`
- `docs/decisions/adr_0010_mvp_synchronous_communication.md`
- `docs/decisions/adr_0011_mvp_single_database_with_schemas.md`

---

## Summary

Архитектура MVP Go2Asia в целом хорошо структурирована и соответствует большинству требований ТЗ. Однако выявлены критические проблемы в области безопасности (защита internal endpoints), data ownership (двойная истина для referral_code), и наблюдаемости (отсутствие обработки каскадных сбоев). Также требуются уточнения по Gateway responsibilities и соответствию ТЗ.

---

## Issues

### 🔴 BLOCKERS (критические проблемы)

#### 1. Security: Защита internal endpoints в Cloudflare Workers

**Проблема:**
В архитектуре указано: "Internal endpoints: Доступны только из внутренней сети Cloudflare Workers, защищены проверкой источника запроса". Однако Cloudflare Workers не имеют концепции "внутренней сети" по умолчанию. Все Workers доступны через публичные URL.

**Детали:**
- `POST /internal/auth/verify-jwt` — как защищён от внешнего доступа?
- `POST /internal/points/add` — кто может вызывать этот endpoint?
- Нет описания механизма service-to-service аутентификации

**Требуемые изменения:**
1. Описать механизм защиты internal endpoints:
   - Использование секретного ключа (Service Secret) в заголовке `X-Service-Secret`
   - Или использование Service JWT токенов (отдельно от User JWT)
   - Или использование Cloudflare Workers Service Bindings (если доступны)
2. Уточнить, где хранятся Service Secrets (Cloudflare Secrets, Environment Variables)
3. Описать процесс ротации Service Secrets

**Файлы для изменения:**
- `docs/architecture/mvp_architecture.md` (раздел 4.2, 8.1)

---

#### 2. Data Ownership: Двойная истина для referral_code

**Проблема:**
В архитектуре `referral_code` хранится в двух местах:
- `auth.users.referral_code` (Auth Service)
- `referral.referral_links.code` (Referral Service)

Это создаёт проблему синхронизации и источник истины не определён.

**Детали:**
- В `mvp_data_schemas.md` (строка 32): `auth.users.referral_code` — "генерируется Referral Service"
- В `mvp_data_schemas.md` (строка 255): `referral.referral_links.code` — "Реферальный код"
- В `mvp_architecture.md` (строка 249): "referral_code (string, unique, nullable) — генерируется Referral Service"
- В `mvp_architecture.md` (строка 471): "Генерирует referral_code (или запрашивает у Referral Service)"

**Требуемые изменения:**
1. Определить единственный источник истины (SSOT):
   - **Вариант A:** Referral Service — единственный владелец `referral_code`. Auth Service хранит только ссылку (опционально для производительности).
   - **Вариант B:** Auth Service — единственный владелец `referral_code`. Referral Service синхронизируется через API.
2. Убрать дублирование: либо убрать `auth.users.referral_code`, либо убрать `referral.referral_links.code` (оставить только один)
3. Описать процесс синхронизации (если всё же нужна денормализация для производительности)

**Файлы для изменения:**
- `docs/architecture/mvp_architecture.md` (раздел 3.2)
- `docs/architecture/mvp_data_schemas.md` (разделы Auth Service и Referral Service)

---

#### 3. Data Ownership: Валидация user_id без внешних ключей

**Проблема:**
В архитектуре указано, что межсервисные связи хранят `user_id` без внешних ключей (например, `event_registrations.user_id`, `points_transactions.user_id`). Однако не описано, как валидируется существование пользователя и целостность данных.

**Детали:**
- `content.event_registrations.user_id` — нет FK, валидация через API
- `points.points_transactions.user_id` — нет FK
- `referral.referral_relations.referrer_user_id` и `referred_user_id` — нет FK

**Требуемые изменения:**
1. Описать стратегию валидации:
   - Проверка существования пользователя через Auth Service API перед записью
   - Или использование "soft validation" (допускаем orphaned records, очищаем периодически)
2. Описать процесс очистки orphaned records (если пользователь удалён)
3. Добавить в схемы комментарии о стратегии валидации

**Файлы для изменения:**
- `docs/architecture/mvp_architecture.md` (раздел 3.1)
- `docs/architecture/mvp_data_schemas.md` (комментарии к таблицам)

---

#### 4. Наблюдаемость: Отсутствие обработки каскадных сбоев

**Проблема:**
В архитектуре описаны синхронные вызовы между сервисами, но не описана обработка ошибок и таймаутов, что может привести к каскадным сбоям.

**Детали:**
- Auth Service → Points Service (синхронный вызов)
- Referral Service → Points Service (синхронный вызов)
- Content Service → Points Service (синхронный вызов)
- Нет описания таймаутов
- Нет описания retry логики
- Нет описания graceful degradation

**Требуемые изменения:**
1. Описать таймауты для межсервисных вызовов (например, 5 секунд)
2. Описать стратегию обработки ошибок:
   - Что делать, если Points Service недоступен при регистрации пользователя?
   - Что делать, если Auth Service недоступен при проверке JWT?
3. Описать retry логику (если применимо)
4. Описать graceful degradation (если сервис недоступен, система продолжает работать с ограниченной функциональностью)

**Файлы для изменения:**
- `docs/architecture/mvp_architecture.md` (раздел 5, 10)
- `docs/decisions/adr_0010_mvp_synchronous_communication.md` (раздел "Митигация рисков")

---

### ⚠️ NON-BLOCKERS (важные замечания)

#### 5. Соответствие ТЗ: Points limits и VIP role

**Проблема:**
В архитектуре не полностью описаны правила начисления Points и роль VIP-Spacer согласно ТЗ.

**Детали:**
- ТЗ требует: "Регистрация на событие: 20 Points, не чаще 1 раза в день"
- В архитектуре указано: "Velocity checks (максимум 1000 Points в час)" — это общий лимит, но не специфичный для каждого типа действия
- ТЗ требует: "VIP-Spacer: повышенные коэффициенты начисления Points за активность рефералов"
- В архитектуре не описано, как реализуются повышенные коэффициенты для VIP-Spacer

**Требуемые изменения:**
1. Уточнить правила начисления Points:
   - Добавить специфичные лимиты для каждого типа действия (registration: 1 раз, event_registration: 1 раз в день)
   - Общий velocity limit (1000 Points в час) — это дополнительная защита
2. Описать реализацию повышенных коэффициентов для VIP-Spacer:
   - Где хранится информация о коэффициентах?
   - Как проверяется роль VIP-Spacer при начислении Points?
   - Какие именно коэффициенты применяются?

**Файлы для изменения:**
- `docs/architecture/mvp_architecture.md` (раздел 2.4, 5.3)

---

#### 6. Gateway Responsibilities: Где происходит authz

**Проблема:**
В архитектуре не чётко определено, где именно происходит авторизация (проверка ролей): на уровне Gateway или на уровне сервиса.

**Детали:**
- В разделе 2.1: "JWT валидация (через Auth Service)" — это аутентификация
- В разделе 4.3: "Проверка ролей на уровне API Gateway или сервиса" — неопределённость
- В разделе 2.3: "Auth Service (для проверки роли администратора)" — проверка на уровне Content Service

**Требуемые изменения:**
1. Чётко определить стратегию авторизации:
   - **Вариант A:** Gateway проверяет JWT и роли, передаёт user_id в сервисы
   - **Вариант B:** Gateway проверяет только JWT, сервисы проверяют роли самостоятельно
2. Описать, как Gateway получает информацию о ролях пользователя (из JWT или через Auth Service)
3. Описать, как сервисы проверяют роли (если проверка на уровне сервиса)

**Файлы для изменения:**
- `docs/architecture/mvp_architecture.md` (раздел 2.1, 4.3, 8.2)

---

#### 7. Gateway Responsibilities: Кэширование и PWA offline

**Проблема:**
В архитектуре описано кэширование на Edge (TTL 600 секунд), но не описано, как это соотносится с PWA offline стратегией.

**Детали:**
- ТЗ требует: "Офлайн-доступ к последним просмотренным местам (кэширование через Service Worker)"
- В архитектуре: "Edge кэш (Cloudflare): Публичные GET запросы: TTL 600 секунд"
- Не описано, как Service Worker синхронизируется с Edge кэшем

**Требуемые изменения:**
1. Описать стратегию кэширования:
   - Edge кэш (Cloudflare) — для быстрого доступа из сети
   - Service Worker кэш (IndexedDB) — для офлайн-доступа
   - Как они взаимодействуют?
2. Уточнить заголовки Cache-Control для разных типов данных:
   - Публичный контент: `Cache-Control: public, s-maxage=600, stale-while-revalidate=600`
   - Персональные данные: `Cache-Control: no-store`
   - Офлайн-контент: `Cache-Control: public, max-age=3600` (для Service Worker)

**Файлы для изменения:**
- `docs/architecture/mvp_architecture.md` (раздел 9.1)

---

#### 8. Наблюдаемость: requestId сквозной трассировки

**Проблема:**
В архитектуре указано, что используется `requestId` для сквозной трассировки, но не описано, как он передаётся между сервисами.

**Детали:**
- В разделе 10.1: "requestId — сквозная трассировка"
- В разделе 2.1: "Логирование всех запросов с requestId"
- Не описано, как Gateway передаёт requestId в сервисы
- Не описано, как сервисы передают requestId в другие сервисы

**Требуемые изменения:**
1. Описать механизм передачи requestId:
   - Gateway генерирует requestId (или получает из заголовка `X-Request-ID`)
   - Gateway передаёт requestId в заголовке `X-Request-ID` при проксировании запросов
   - Сервисы передают requestId в заголовке `X-Request-ID` при межсервисных вызовах
2. Описать формат requestId (UUID v4)
3. Описать, как requestId логируется в каждом сервисе

**Файлы для изменения:**
- `docs/architecture/mvp_architecture.md` (раздел 10.1)

---

#### 9. Соответствие ТЗ: Seed данные

**Проблема:**
В архитектуре описаны требования к seed данным, но не описано, как они загружаются и когда.

**Детали:**
- В `mvp_data_schemas.md` описаны требования к seed данным
- Не описано, когда загружаются seed данные (при деплое, вручную, автоматически)
- Не описано, как seed данные связаны с миграциями

**Требуемые изменения:**
1. Описать процесс загрузки seed данных:
   - Seed данные загружаются после миграций
   - Seed данные можно запускать отдельной командой (`pnpm seed`)
   - Seed данные идемпотентны (можно запускать многократно)
2. Описать, где хранятся seed данные (в репозитории, в отдельном хранилище)

**Файлы для изменения:**
- `docs/architecture/mvp_data_schemas.md` (раздел "Seed данные")

---

## Required Changes

### Критические изменения (BLOCKERS)

1. **Добавить раздел "Service-to-Service Authentication"** в `mvp_architecture.md`:
   - Описать механизм защиты internal endpoints (Service Secret или Service JWT)
   - Описать, где хранятся Service Secrets
   - Описать процесс ротации Service Secrets

2. **Исправить data ownership для referral_code**:
   - Определить единственный источник истины (рекомендуется: Referral Service)
   - Убрать дублирование из `auth.users.referral_code` (оставить только в Referral Service)
   - Описать процесс синхронизации (если нужна денормализация)

3. **Добавить раздел "Data Validation Strategy"** в `mvp_architecture.md`:
   - Описать стратегию валидации user_id без FK
   - Описать процесс очистки orphaned records

4. **Расширить раздел "Наблюдаемость"** в `mvp_architecture.md`:
   - Добавить описание таймаутов для межсервисных вызовов
   - Добавить описание стратегии обработки ошибок
   - Добавить описание graceful degradation

### Важные изменения (NON-BLOCKERS)

5. **Уточнить правила начисления Points** в `mvp_architecture.md`:
   - Добавить специфичные лимиты для каждого типа действия
   - Описать реализацию повышенных коэффициентов для VIP-Spacer

6. **Чётко определить стратегию авторизации** в `mvp_architecture.md`:
   - Определить, где происходит проверка ролей (Gateway vs сервис)
   - Описать, как Gateway получает информацию о ролях

7. **Уточнить стратегию кэширования** в `mvp_architecture.md`:
   - Описать взаимодействие Edge кэша и Service Worker кэша
   - Уточнить заголовки Cache-Control

8. **Описать механизм передачи requestId** в `mvp_architecture.md`:
   - Описать, как requestId передаётся между сервисами
   - Описать формат requestId

9. **Уточнить процесс загрузки seed данных** в `mvp_data_schemas.md`:
   - Описать, когда и как загружаются seed данные

---

## Verdict

**Статус:** **CHANGES REQUIRED**

**Обоснование:**

Архитектура MVP Go2Asia в целом хорошо структурирована и соответствует большинству требований ТЗ. Однако выявлены критические проблемы в области безопасности (защита internal endpoints), data ownership (двойная истина для referral_code), и наблюдаемости (отсутствие обработки каскадных сбоев), которые должны быть исправлены перед началом реализации.

**Рекомендуемые действия:**

1. Исправить все BLOCKER issues перед началом реализации
2. Исправить NON-BLOCKER issues перед началом реализации (желательно)
3. После исправления — повторное ревью архитектуры

---

## Deliverables

- ✅ Ревью архитектуры MVP проведено
- ✅ Выявлены 4 критические проблемы (BLOCKERS)
- ✅ Выявлены 5 важных замечаний (NON-BLOCKERS)
- ✅ Вердикт выдан: CHANGES REQUIRED

## Open Questions

Нет открытых вопросов. Все проблемы описаны в разделе Issues.

## Next

После исправления всех BLOCKER issues архитектор должен повторно отправить архитектуру на ревью. После получения статуса APPROVED архитектура может быть передана Planner для создания плана реализации MVP.







